/* v0.5.2 — streaming world + discovery map + live crash nav + clean ADS */
const V052_CHUNK=96;
const V052_ACTIVE_RADIUS=2;
const V052_MAP_KEY='vast_dead_zone_discovery_v052';
const v052Chunks=new Map(),v052ProcColliders=new Set();
let v052StreamAt=0,v052MapOpen=false,v052MapRAF=0;
const v052MapState=(()=>{try{const x=JSON.parse(localStorage.getItem(V052_MAP_KEY)||'null');return x&&typeof x==='object'?x:{chunks:[],pois:{}}}catch{return {chunks:[],pois:{}}}})();
const v052Discovered=new Set(v052MapState.chunks||[]);
if(!v052MapState.pois)v052MapState.pois={};
const v052FixedPOI=[
 {name:'Gasolinera',type:'civil',x:-48,z:20},{name:'Clínica',type:'medical',x:50,z:48},
 {name:'Comisaría',type:'police',x:-72,z:-48},{name:'Granja',type:'farm',x:28,z:-72},
 {name:'Base militar',type:'military',x:78,z:-72}
];
function v052Key(cx,cz){return `${cx},${cz}`}
function v052ChunkCoord(v){return Math.floor(v/V052_CHUNK)}
function v052Hash(cx,cz,s=0){let h=(Math.imul(cx,374761393)^Math.imul(cz,668265263)^Math.imul(s+1,2246822519))>>>0;h=Math.imul(h^(h>>>13),1274126177)>>>0;return (h^(h>>>16))>>>0}
function v052Rng(cx,cz,s=0){let a=v052Hash(cx,cz,s)||1;return()=>{a+=0x6D2B79F5;let t=a;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return((t^(t>>>14))>>>0)/4294967296}}
function v052SaveMap(){v052MapState.chunks=[...v052Discovered];try{localStorage.setItem(V052_MAP_KEY,JSON.stringify(v052MapState))}catch{}}
function v052Box(group,w,h,d,x,y,z,mat){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),mat);m.position.set(x,y,z);m.castShadow=h>.7;m.receiveShadow=true;group.add(m);return m}
function v052Collider(chunk,minX,maxX,minZ,maxZ,tag='proc'){const o={minX,maxX,minZ,maxZ,active:true,tag};chunk.colliders.push(o);v052ProcColliders.add(o);return o}
function v052Chest(chunk,id,label,x,z,defaults){
 const g=new THREE.Group();g.position.set(x,.38,z);const base=new THREE.Mesh(new THREE.BoxGeometry(1.05,.65,.72),new THREE.MeshStandardMaterial({color:0x4a514c,roughness:.82,metalness:.22})),lid=new THREE.Mesh(new THREE.BoxGeometry(1.08,.13,.75),new THREE.MeshStandardMaterial({color:0x68716a,roughness:.74,metalness:.28}));lid.position.y=.39;g.add(base,lid);chunk.group.add(g);
 const c={type:'container',id,label,x,z,group:g,lid,items:Object.assign({},containerSave[id]??defaults)};containers.push(c);chunk.containers.push(c);v052Collider(chunk,x-.56,x+.56,z-.41,z+.41,'proc-container');return c
}
function v052POIName(type,cx,cz){const names={village:['Pueblo Viejo','Casas del Pinar','Barrio Abandonado','Los Olivos'],industrial:['Polígono Oxidado','Depósito Norte','Patio Ferroviario','Fábrica Gris'],military:['Puesto Echo','Campamento Kilo','Control Bravo','Puesto Sierra'],forest:['Bosque Cerrado','Pinar Quemado','Monte Bajo','Sendero Perdido'],scrub:['Llanura Seca','Terreno Baldío','Campo Abierto','Cruce Viejo']};const a=names[type]||names.scrub;return a[v052Hash(cx,cz,91)%a.length]}
function v052GenerateChunk(cx,cz){
 const key=v052Key(cx,cz);if(v052Chunks.has(key))return v052Chunks.get(key);const rnd=v052Rng(cx,cz),group=new THREE.Group(),chunk={cx,cz,key,group,colliders:[],containers:[],poi:[]};scene.add(group);v052Chunks.set(key,chunk);
 const minX=cx*V052_CHUNK,minZ=cz*V052_CHUNK,cxw=minX+V052_CHUNK/2,czw=minZ+V052_CHUNK/2;
 const ground=new THREE.Mesh(new THREE.PlaneGeometry(V052_CHUNK+.6,V052_CHUNK+.6),new THREE.MeshStandardMaterial({color:0x77705f,roughness:1}));ground.rotation.x=-Math.PI/2;ground.position.set(cxw,-.004,czw);ground.receiveShadow=true;group.add(ground);
 const core=Math.abs(cxw)<175&&Math.abs(czw)<175;if(core)return chunk;
 const roll=rnd(),type=roll<.18?'village':roll<.30?'industrial':roll<.37?'military':roll<.68?'forest':'scrub';
 const roadMat=new THREE.MeshStandardMaterial({color:0x444947,roughness:.96});if(rnd()<.46){const vertical=rnd()<.5,r=new THREE.Mesh(new THREE.PlaneGeometry(vertical?9:V052_CHUNK,vertical?V052_CHUNK:9),roadMat);r.rotation.x=-Math.PI/2;r.position.set(cxw,.007,czw);group.add(r)}
 const trunkMat=new THREE.MeshStandardMaterial({color:0x544838,roughness:1}),leafMat=new THREE.MeshStandardMaterial({color:0x3c493d,roughness:1}),rockMat=new THREE.MeshStandardMaterial({color:0x686862,roughness:.94});
 const treeCount=type==='forest'?14:type==='scrub'?6:4;for(let i=0;i<treeCount;i++){const x=minX+8+rnd()*(V052_CHUNK-16),z=minZ+8+rnd()*(V052_CHUNK-16);if(Math.hypot(x-cxw,z-czw)<10&&type!=='forest')continue;const s=.7+rnd()*.65,g=new THREE.Group(),t=new THREE.Mesh(new THREE.CylinderGeometry(.16*s,.28*s,3.8*s,7),trunkMat);t.position.y=1.9*s;const c=new THREE.Mesh(new THREE.DodecahedronGeometry(.95*s,0),leafMat);c.position.y=3.9*s;c.scale.y=1.25;g.add(t,c);g.position.set(x,0,z);group.add(g);v052Collider(chunk,x-.28*s,x+.28*s,z-.28*s,z+.28*s,'proc-tree')}
 for(let i=0;i<3+Math.floor(rnd()*4);i++){const x=minX+6+rnd()*(V052_CHUNK-12),z=minZ+6+rnd()*(V052_CHUNK-12),r=.4+rnd()*.55,m=new THREE.Mesh(new THREE.DodecahedronGeometry(r,0),rockMat);m.position.set(x,r*.55,z);m.rotation.set(rnd(),rnd()*6.2,rnd());group.add(m);v052Collider(chunk,x-r*.6,x+r*.6,z-r*.6,z+r*.6,'proc-rock')}
 const wall=new THREE.MeshStandardMaterial({color:type==='military'?0x566258:type==='industrial'?0x656a67:0x817565,roughness:.9}),roof=new THREE.MeshStandardMaterial({color:0x4b4c47,roughness:.8,metalness:type==='industrial'?.18:.03});
 const addHouse=(x,z,w,d,h)=>{v052Box(group,w,h,d,x,h/2,z,wall);v052Box(group,w+.35,.22,d+.35,x,h+.11,z,roof);v052Collider(chunk,x-w/2,x+w/2,z-d/2,z+d/2,'proc-building')};
 if(type==='village'){for(let i=0;i<3;i++)addHouse(minX+22+i*22+rnd()*4,czw+(i%2?13:-13),13+rnd()*4,10+rnd()*3,3.8+rnd()*1.5)}
 if(type==='industrial'){addHouse(cxw-12,czw,30,18,6);for(let i=0;i<4;i++){const x=cxw+17+(i%2)*8,z=czw-12+Math.floor(i/2)*13;v052Box(group,6,2.5,2.6,x,1.25,z,new THREE.MeshStandardMaterial({color:[0x4d5d58,0x75503f,0x555d68][i%3],roughness:.75,metalness:.18}));v052Collider(chunk,x-3,x+3,z-1.3,z+1.3,'proc-container')}}
 if(type==='military'){addHouse(cxw,czw,19,13,4.4);for(const [ox,oz] of [[-18,-16],[18,-16],[-18,16],[18,16]]){const p=new THREE.Mesh(new THREE.CylinderGeometry(.09,.12,4.2,7),v05Metal());p.position.set(cxw+ox,2.1,czw+oz);group.add(p)}}
 if(['village','industrial','military'].includes(type)){
   const poi={name:v052POIName(type,cx,cz),type,x:cxw,z:czw};chunk.poi.push(poi);v052MapState.pois[key]=[poi];
   const loot=type==='military'?{ammo556:45,ammo762:30,bandage:2,water:1}:type==='industrial'?{metal:18,scrap:12,water:1,ammo9:12}:{food:2,water:2,bandage:1,ammo9:18};v052Chest(chunk,`proc-cache-${key}`,'Alijo de '+poi.name,cxw+8,czw+6,loot)
 }
 return chunk
}
function v052UnloadChunk(key){const c=v052Chunks.get(key);if(!c)return;c.colliders.forEach(o=>v052ProcColliders.delete(o));c.containers.forEach(x=>{const i=containers.indexOf(x);if(i>=0)containers.splice(i,1)});scene.remove(c.group);v052Chunks.delete(key)}
function v052Discover(cx,cz){const key=v052Key(cx,cz);if(v052Discovered.has(key))return;v052Discovered.add(key);const c=v052Chunks.get(key);if(c?.poi?.length)v052MapState.pois[key]=c.poi;v052SaveMap();toast('ZONA DESCUBIERTA · '+(c?.poi?.[0]?.name||`SECTOR ${cx}:${cz}`),'good')}
function v052UpdateStreaming(force=false){
 const t=performance.now();if(!force&&t<v052StreamAt)return;v052StreamAt=t+350;const cx=v052ChunkCoord(player.pos.x),cz=v052ChunkCoord(player.pos.z);v052Discover(cx,cz);
 for(let dz=-V052_ACTIVE_RADIUS;dz<=V052_ACTIVE_RADIUS;dz++)for(let dx=-V052_ACTIVE_RADIUS;dx<=V052_ACTIVE_RADIUS;dx++)v052GenerateChunk(cx+dx,cz+dz);
 for(const [key,c] of [...v052Chunks])if(Math.abs(c.cx-cx)>V052_ACTIVE_RADIUS+1||Math.abs(c.cz-cz)>V052_ACTIVE_RADIUS+1)v052UnloadChunk(key)
}

/* No hard square wall: collision now streams forever with the active chunks. */
collidesAt=function(x,z,r=.42){for(const o of obstacles)if(o.active&&x+r>o.minX&&x-r<o.maxX&&z+r>o.minZ&&z-r<o.maxZ)return true;for(const o of v052ProcColliders)if(o.active&&x+r>o.minX&&x-r<o.maxX&&z+r>o.minZ&&z-r<o.maxZ)return true;return false};
clearLine=function(ax,az,bx,bz){const steps=Math.ceil(Math.hypot(bx-ax,bz-az)/1.4),all=[...obstacles,...v052ProcColliders];for(let i=1;i<steps;i++){const t=i/steps,x=lerp(ax,bx,t),z=lerp(az,bz,t);for(const o of all){if(!o.active||o.tag==='car'||o.tag==='container'||o.tag==='proc-tree'||o.tag==='proc-rock')continue;if(x>o.minX&&x<o.maxX&&z>o.minZ&&z<o.maxZ)return false}}return true};
randomSpawn=function(min=28,max=80){for(let i=0;i<35;i++){const a=Math.random()*Math.PI*2,d=rand(min,max),x=player.pos.x+Math.cos(a)*d,z=player.pos.z+Math.sin(a)*d;if(!collidesAt(x,z,.7))return new THREE.Vector3(x,0,z)}return new THREE.Vector3(player.pos.x+rand(-55,55),0,player.pos.z+rand(-55,55))};
v05BuildReason=function(type,x,z,rot){const r=BUILD_RECIPES[type];if(!canAfford(r.cost))return 'FALTAN RECURSOS';if(type==='door'){const frame=v05NearestDoorFrame(x,z);if(!frame)return 'NECESITA PARED CON PUERTA';if(structures.some(s=>s.type==='door'&&Math.hypot(s.x-frame.x,s.z-frame.z)<.7))return 'YA HAY UNA PUERTA EN ESTE MARCO';return ''}if(type==='roof'&&!v05RoofSupport(x,z))return 'EL TECHO NECESITA PAREDES CERCA';if(type==='roof'&&structures.some(s=>s.type==='roof'&&Math.hypot(s.x-x,s.z-z)<1.2))return 'YA HAY UN TECHO AQUÍ';if(type!=='roof'&&v05StaticBlocked(x,z,r.w,r.d,rot))return 'ESPACIO OCUPADO';if(v05StructureTooClose(type,x,z))return 'DEMASIADO CERCA DE OTRA ESTRUCTURA';if(Math.hypot(player.pos.x-x,player.pos.z-z)<1.45)return 'APÁRTATE PARA CONSTRUIR';return ''};

/* Crash events land on streamed terrain close enough to be reachable, not under/behind the old map boundary. */
triggerHeliEvent=function(){if(heliEvent?.active)return;let x=player.pos.x,z=player.pos.z;for(let i=0;i<24;i++){const a=Math.random()*Math.PI*2,d=rand(75,135),tx=player.pos.x+Math.cos(a)*d,tz=player.pos.z+Math.sin(a)*d;if(!collidesAt(tx,tz,3)){x=tx;z=tz;break}}heliEvent={active:true,x,z,looted:false};v052UpdateStreaming(true);buildHeliCrash(x,z);setObjective(5);toast('¡HELICÓPTERO CAÍDO! · MIRA EL MAPA','bad');saveGame()};
const v052BuildCrashBase=buildHeliCrash;
buildHeliCrash=function(x,z){v052BuildCrashBase(x,z);const ring=new THREE.Mesh(new THREE.RingGeometry(2.1,2.75,32),new THREE.MeshBasicMaterial({color:0xe26b37,transparent:true,opacity:.58,side:THREE.DoubleSide,depthWrite:false}));ring.rotation.x=-Math.PI/2;ring.position.set(x,.035,z);scene.add(ring);const flare=new THREE.PointLight(0xff6633,2.2,32,2);flare.position.set(x,3.2,z);scene.add(flare)};

/* Live navigator: uses the actual player/yaw every frame, not a 7-second-old save. */
function v052UpdateCrashNav(){const n=$('heli-nav-v052');if(!n)return;if(!heliEvent?.active||heliEvent.looted){n.classList.remove('show');return}const dx=heliEvent.x-player.pos.x,dz=heliEvent.z-player.pos.z,d=Math.hypot(dx,dz),target=Math.atan2(-dx,-dz);let rel=target-yaw;while(rel>Math.PI)rel-=Math.PI*2;while(rel<-Math.PI)rel+=Math.PI*2;const a=n.querySelector('.v052-nav-arrow'),s=n.querySelector('small');if(a)a.style.transform=`rotate(${rel*180/Math.PI}deg)`;if(s)s.textContent=d<12?'LLEGASTE · BUSCA LA CAJA':`${Math.round(d)} m · SIGUE LA FLECHA`;n.classList.add('show')}

/* Pilot helicopter can leave the original 330 m square and keeps streaming terrain. */
v05UpdateFlyHeli=function(dt){if(!flyHeli)return;const h=flyHeli;h.rotor.rotation.y+=dt*(h.engine?25:1.2);h.tailRotor.rotation.z+=dt*(h.engine?31:1.5);if(!inFlyHeli)return;const f=touchMove.y+(keys.has('KeyW')?1:0)-(keys.has('KeyS')?1:0),turn=touchMove.x+(keys.has('KeyD')?1:0)-(keys.has('KeyA')?1:0),vert=(heliUpHeld||keys.has('Space')?1:0)-(heliDownHeld||keys.has('ControlLeft')?1:0);h.heading-=turn*dt*1.32;h.group.rotation.y=h.heading;h.group.position.y=clamp(h.group.position.y+vert*dt*5.2,.78,28);const power=h.fuel>0?1:0,speed=clamp(f,-1,1)*(h.group.position.y>1.7?12.5:5.2)*power,dx=-Math.sin(h.heading)*speed*dt,dz=-Math.cos(h.heading)*speed*dt;if(h.group.position.y>2.1||!collidesAt(h.group.position.x+dx,h.group.position.z+dz,1.05)){h.group.position.x+=dx;h.group.position.z+=dz}h.fuel=clamp(h.fuel-dt*(.035+Math.abs(f)*.05+Math.abs(vert)*.04)*power,0,100);if(h.fuel<=0){h.group.position.y=Math.max(.78,h.group.position.y-dt*1.4);heliUpHeld=false;heliDownHeld=false}player.pos.set(h.group.position.x,h.group.position.y+1.05,h.group.position.z);yaw=h.heading;pitch=lerp(pitch,-.08,clamp(dt*3,0,1));yawNode.position.set(player.pos.x,player.pos.y,player.pos.z);yawNode.rotation.y=yaw;pitchNode.rotation.x=pitch;camera.fov=lerp(camera.fov,76,clamp(dt*4,0,1));camera.updateProjectionMatrix();noise=.9;player.stamina=clamp(player.stamina+dt*15,0,100);const alt=$('heli-alt'),fuel=$('heli-fuel');if(alt)alt.textContent=Math.max(0,Math.round(h.group.position.y-.78))+' m';if(fuel)fuel.textContent=Math.round(h.fuel)+'%';v052UpdateStreaming()};

function v052DrawMap(){if(!v052MapOpen)return;const c=$('v052-map-canvas');if(!c)return;const rect=c.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2),w=Math.max(320,Math.round(rect.width*dpr)),h=Math.max(220,Math.round(rect.height*dpr));if(c.width!==w||c.height!==h){c.width=w;c.height=h}const g=c.getContext('2d');g.setTransform(dpr,0,0,dpr,0,0);const Wc=rect.width,Hc=rect.height;g.fillStyle='#0b100f';g.fillRect(0,0,Wc,Hc);const pts=[...v052Discovered].map(k=>k.split(',').map(Number));if(!pts.length)return;let minX=Math.min(...pts.map(p=>p[0])),maxX=Math.max(...pts.map(p=>p[0])),minZ=Math.min(...pts.map(p=>p[1])),maxZ=Math.max(...pts.map(p=>p[1]));const pcx=v052ChunkCoord(player.pos.x),pcz=v052ChunkCoord(player.pos.z);minX=Math.min(minX,pcx)-1;maxX=Math.max(maxX,pcx)+1;minZ=Math.min(minZ,pcz)-1;maxZ=Math.max(maxZ,pcz)+1;const sx=(Wc-54)/(maxX-minX+1),sz=(Hc-54)/(maxZ-minZ+1),cell=Math.max(8,Math.min(sx,sz)),ox=(Wc-(maxX-minX+1)*cell)/2,oz=(Hc-(maxZ-minZ+1)*cell)/2;const mapX=x=>ox+(x/V052_CHUNK-minX)*cell,mapZ=z=>oz+(z/V052_CHUNK-minZ)*cell;
 for(const [cx,cz] of pts){const x=ox+(cx-minX)*cell,z=oz+(cz-minZ)*cell;g.fillStyle='#29312e';g.fillRect(x+.5,z+.5,cell-1,cell-1);g.strokeStyle='rgba(255,255,255,.055)';g.strokeRect(x+.5,z+.5,cell-1,cell-1)}
 const pois=[...v052FixedPOI];for(const arr of Object.values(v052MapState.pois||{}))if(Array.isArray(arr))pois.push(...arr);const seen=new Set();for(const p of pois){const ck=v052Key(v052ChunkCoord(p.x),v052ChunkCoord(p.z));if(!v052Discovered.has(ck))continue;const id=p.name+'@'+p.x+','+p.z;if(seen.has(id))continue;seen.add(id);const x=mapX(p.x),z=mapZ(p.z);g.fillStyle=p.type==='military'?'#c66b58':p.type==='medical'?'#77b991':p.type==='industrial'?'#b89968':'#d3b178';g.beginPath();g.arc(x,z,3.4,0,Math.PI*2);g.fill();if(cell>15){g.font='600 8px system-ui';g.fillStyle='#c9ceca';g.fillText(p.name,x+6,z+3)}}
 if(heliEvent?.active&&!heliEvent.looted){const x=mapX(heliEvent.x),z=mapZ(heliEvent.z);g.strokeStyle='#ef7041';g.lineWidth=2;g.beginPath();g.arc(x,z,7,0,Math.PI*2);g.stroke();g.fillStyle='#ef7041';g.font='700 9px system-ui';g.fillText('HELI CAÍDO',x+10,z+3)}
 if(flyHeli){const x=mapX(flyHeli.group.position.x),z=mapZ(flyHeli.group.position.z);g.fillStyle='#65b9d9';g.fillRect(x-3,z-3,6,6)}
 const px=mapX(player.pos.x),pz=mapZ(player.pos.z),ang=yaw,fx=-Math.sin(ang),fz=-Math.cos(ang);g.fillStyle='#f2f0e7';g.beginPath();g.arc(px,pz,4.3,0,Math.PI*2);g.fill();g.strokeStyle='#5ec7ff';g.lineWidth=2;g.beginPath();g.moveTo(px,pz);g.lineTo(px+fx*15,pz+fz*15);g.stroke();g.fillStyle='#87918d';g.font='600 8px system-ui';g.fillText(`${Math.round(player.pos.x)}, ${Math.round(player.pos.z)}`,10,Hc-10);v052MapRAF=requestAnimationFrame(v052DrawMap)}
function v052OpenMap(){if(dead||inventoryOpen)return;if(buildMode)cancelBuild();v052MapOpen=true;paused=true;fireHeld=false;$('v052-map-overlay').classList.remove('hidden');v052DrawMap()}
function v052CloseMap(){if(!v052MapOpen)return;v052MapOpen=false;cancelAnimationFrame(v052MapRAF);$('v052-map-overlay').classList.add('hidden');paused=false;clock.getDelta();AudioFX.resume()}

function v052SetupUI(){
 const old=$('heli-nav');if(old)old.style.display='none';
 const hud=$('hud'),nav=document.createElement('div');nav.id='heli-nav-v052';nav.innerHTML='<span class="v052-nav-arrow">↑</span><span><b>HELICÓPTERO CAÍDO</b><small>LOCALIZANDO…</small></span>';document.body.appendChild(nav);
 const sight=document.createElement('div');sight.id='v052-ads-sight';sight.innerHTML='<div class="v052-iron"><i class="rear l"></i><i class="rear r"></i><i class="front"></i><i class="front-base"></i></div><div class="v052-rds"><i></i><b></b></div>';hud.appendChild(sight);
 const mb=document.createElement('button');mb.id='map-btn';mb.className='glass';mb.textContent='MAPA';hud.appendChild(mb);mb.addEventListener('pointerdown',e=>{e.stopPropagation();v052OpenMap()});
 const ov=document.createElement('section');ov.id='v052-map-overlay';ov.className='hidden';ov.innerHTML='<div class="v052-map-shell"><header><div><small>EXPLORACIÓN</small><h2>MAPA DE LA ZONA</h2></div><button id="v052-map-close">✕</button></header><canvas id="v052-map-canvas"></canvas><footer><span>■ Tú + dirección · ● zonas descubiertas · □ helicóptero pilotable</span><button id="v052-map-close-2">VOLVER</button></footer></div>';document.body.appendChild(ov);$('v052-map-close').onclick=v052CloseMap;$('v052-map-close-2').onclick=v052CloseMap;
 const st=document.createElement('style');st.textContent=`
 #heli-nav{display:none!important}#heli-nav-v052{position:fixed;z-index:74;left:50%;top:43px;transform:translateX(-50%);display:none;align-items:center;gap:7px;padding:5px 9px;border:1px solid rgba(230,108,55,.45);border-radius:4px;background:rgba(7,9,8,.86);pointer-events:none;font:800 9px 'Barlow Condensed',sans-serif;color:#eee}#heli-nav-v052.show{display:flex}.v052-nav-arrow{display:flex;width:21px;height:21px;align-items:center;justify-content:center;border:1px solid #df7044;border-radius:50%;color:#ff9160;font-size:16px}.v052-nav-arrow+span{display:flex;flex-direction:column}.v052-nav-arrow+span small{font-size:6px;color:#aeb4ae;margin-top:2px}
 #map-btn{pointer-events:auto;position:absolute;z-index:16;top:calc(8px + var(--safe-t));right:calc(198px + var(--safe-r));height:30px;padding:0 10px;border-radius:3px;color:#9ed8ef;font:800 8px 'Barlow Condensed',sans-serif;letter-spacing:.09em;border:1px solid rgba(79,173,214,.32)}
 #v052-map-overlay{position:fixed;z-index:210;inset:0;display:flex;align-items:center;justify-content:center;padding:6px;background:rgba(2,4,4,.9);backdrop-filter:blur(9px);-webkit-backdrop-filter:blur(9px)}#v052-map-overlay.hidden{display:none!important}.v052-map-shell{width:min(1100px,96vw);height:min(670px,96dvh);display:grid;grid-template-rows:48px minmax(0,1fr) 38px;border:1px solid rgba(255,255,255,.12);border-top:2px solid #4eabcF;background:#0a0e0d;box-shadow:0 25px 80px #000;overflow:hidden}.v052-map-shell header,.v052-map-shell footer{display:flex;align-items:center;justify-content:space-between;padding:6px 12px}.v052-map-shell header{border-bottom:1px solid rgba(255,255,255,.08)}.v052-map-shell header small{font:700 7px Inter,sans-serif;color:#62bce0;letter-spacing:.18em}.v052-map-shell h2{margin:1px 0 0;font:800 20px 'Barlow Condensed',sans-serif}.v052-map-shell header button{width:30px;height:30px;background:#151a18;color:#aaa}.v052-map-shell canvas{width:100%;height:100%;display:block;background:#0b100f}.v052-map-shell footer{border-top:1px solid rgba(255,255,255,.08);font:600 7px Inter,sans-serif;color:#7e8782}.v052-map-shell footer button{height:26px;padding:0 13px;background:#174d66;color:#dff4fc;font:800 8px 'Barlow Condensed',sans-serif}
 #v052-ads-sight{position:absolute;z-index:19;left:50%;top:50%;width:120px;height:120px;transform:translate(-50%,-50%);pointer-events:none;display:none}body.ads-active #v052-ads-sight{display:block}body.ads-active #crosshair{display:none!important}.v052-iron,.v052-rds{position:absolute;inset:0}.v052-rds{display:none}.v052-iron .rear{position:absolute;top:55px;width:5px;height:28px;background:linear-gradient(#303735,#0c0e0d);border:1px solid rgba(255,255,255,.08)}.v052-iron .rear.l{left:37px;transform:rotate(7deg)}.v052-iron .rear.r{right:37px;transform:rotate(-7deg)}.v052-iron .front{position:absolute;left:58px;top:51px;width:4px;height:24px;background:#111514;border:1px solid #46504b}.v052-iron .front-base{position:absolute;left:50px;top:72px;width:20px;height:4px;background:#151a18}.v052-rds:before{content:'';position:absolute;left:31px;top:31px;width:56px;height:56px;border:8px solid #131918;border-radius:50%;box-shadow:0 0 0 2px #394440,inset 0 0 16px rgba(92,174,188,.12)}.v052-rds i{position:absolute;left:39px;top:39px;width:40px;height:40px;border-radius:50%;background:rgba(82,151,163,.07)}.v052-rds b{position:absolute;left:58px;top:58px;width:5px;height:5px;border-radius:50%;background:#ff3827;box-shadow:0 0 8px #ff3827}body.ads-rds .v052-iron{display:none}body.ads-rds .v052-rds{display:block}
 @media (orientation:landscape) and (max-height:520px){#map-btn{right:calc(194px + var(--safe-r));height:29px;font-size:7px}.v052-map-shell{height:96dvh;grid-template-rows:40px minmax(0,1fr) 31px}.v052-map-shell h2{font-size:16px}.v052-map-shell footer{font-size:6px}#v052-ads-sight{transform:translate(-50%,-50%) scale(.9)}}`;
 document.head.appendChild(st)
}

const v052UpdatePlayerBase=updatePlayer;
updatePlayer=function(dt){v052UpdatePlayerBase(dt);v052UpdateStreaming();v052UpdateCrashNav();const ads=aimHeld&&!inFlyHeli;if(weaponRig&&ads){weaponRig.position.x=lerp(weaponRig.position.x,0,clamp(dt*15,0,1));weaponRig.position.y=lerp(weaponRig.position.y,-.31,clamp(dt*15,0,1));weaponRig.position.z=lerp(weaponRig.position.z,-.48,clamp(dt*15,0,1));weaponRig.scale.setScalar(lerp(weaponRig.scale.x,.86,clamp(dt*12,0,1)))}else if(weaponRig&&!inFlyHeli)weaponRig.scale.setScalar(lerp(weaponRig.scale.x,1,clamp(dt*10,0,1)));document.body.classList.toggle('ads-rds',ads&&!!player.attachments.reddot&&['mp5','m4','ak'].includes(W[player.weapon].id));if($('v052-ads-sight'))$('v052-ads-sight').style.display=ads?'block':'none'};

const v052SetupV05Base=setupV05;
setupV05=function(){v052SetupV05Base();v052SetupUI();v052UpdateStreaming(true);setTimeout(()=>{buildWeapon();v052UpdateCrashNav()},0)};
