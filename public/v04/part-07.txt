og.color.copy(tmpColor);scene.fog.density=lerp(.012,.0068,light);lights.forEach(l=>l.intensity=light<.35?(1-light)*1.45:0);$('day-label').textContent=`DÍA ${day}`;$('day-value').textContent=day;$('clock-label').textContent=`${String(Math.floor(hour)).padStart(2,'0')}:${String(Math.floor((hour%1)*60)).padStart(2,'0')}`}
function spawnDirector(dt){spawnTick+=dt;eventTick+=dt;const alive=zombies.filter(z=>z.alive).length,cap=settings.quality==='low'?15:settings.quality==='high'?26:20;if(spawnTick>Math.max(5,10-player.kills*.03)&&alive<cap){spawnTick=0;const roll=Math.random();spawnZombie(randomSpawn(30,75),roll<.14?'runner':roll>.92?'military':'walker')}if(!heliEvent?.active&&runTime>70&&eventTick>45){eventTick=0;triggerHeliEvent()}}

function updateUI(force=false){if(!force&&uiTick<.1)return;uiTick=0;$('hp-bar').style.width=player.hp+'%';$('hunger-bar').style.width=player.hunger+'%';$('thirst-bar').style.width=player.thirst+'%';$('stamina-bar').style.width=player.stamina+'%';$('hp-value').textContent=Math.ceil(player.hp);$('hunger-value').textContent=Math.ceil(player.hunger);$('thirst-value').textContent=Math.ceil(player.thirst);$('kills-value').textContent=player.kills;$('noise-value').textContent=Math.round(noise*100)+'%';$('medkit-count').textContent='x'+(player.inventory.medkit||0);$('food-count').textContent='x'+(player.inventory.food||0);$('water-count').textContent='x'+(player.inventory.water||0);const nearby=zombies.filter(z=>z.alive&&dist(z.group.position,player.pos)<16).length;$('threat-label').textContent=nearby>5?'AMENAZA CRÍTICA':nearby>2?'AMENAZA ALTA':'AMENAZA BAJA';$('threat-label').style.color=nearby>5?'#dc6452':nearby>2?'#d49a4e':'#9fbc87';const bleed=$('bleed-status');if(bleed)bleed.classList.toggle('active',player.bleeding>0);updateAmmoUI();updateBagButton();updateResourceHUD()}
function updateAmmoUI(){const w=W[player.weapon],res=player.inventory[w.ammo]||0;$('weapon-name').textContent=w.label;$('ammo-mag').textContent=w.mag;$('ammo-reserve').textContent=res;$('fire-mode').textContent=reloadUntil?'RECARGANDO':(player.attachments.suppressor&&['m9','mp5','m4'].includes(w.id)?w.mode+' · SUP':w.mode);const slots=document.querySelectorAll('.slot[data-slot]');slots.forEach((b,i)=>{const wi=W[i];if(!wi){b.style.display='none';return}b.classList.toggle('locked',!wi.unlocked);const small=b.querySelector('small');if(small)small.textContent=wi.unlocked?`${wi.mag}/${player.inventory[wi.ammo]||0}`:'BLOQ.'})}
function updateWeaponSlots(){document.querySelectorAll('.slot[data-slot]').forEach((b,i)=>{if(!W[i])return;b.classList.toggle('locked',!W[i].unlocked);b.classList.toggle('active',i===player.weapon);const label=b.querySelector('b');if(label)label.textContent=W[i].label})}
function showHitmarker(kill=false){const h=$('hitmarker');h.className=kill?'show kill':'show';clearTimeout(showHitmarker.t);showHitmarker.t=setTimeout(()=>h.className='',110)}
function toast(text,type=''){const stack=$('toast-stack'),el=document.createElement('div');el.className='toast '+type;el.textContent=text;stack.appendChild(el);setTimeout(()=>{el.style.opacity='0';el.style.transform='translateY(-4px)';el.style.transition='.25s';setTimeout(()=>el.remove(),260)},2200)}

function setupProUI(){
 const hud=$('hud'),invBtn=document.createElement('button');invBtn.id='inventory-btn';invBtn.className='glass';invBtn.innerHTML='<span>MOCHILA</span><b id="bag-status">0/10</b>';hud.appendChild(invBtn);const st=document.createElement('div');st.id='status-strip';st.innerHTML='<span id="bleed-status">SANGRADO</span><span id="armor-status">SIN ARMADURA</span><span class="save-dot">AUTO</span>';hud.appendChild(st);const buildBtn=document.createElement('button');buildBtn.id='build-btn';buildBtn.className='glass';buildBtn.textContent='CONSTRUIR';hud.appendChild(buildBtn);const rh=document.createElement('div');rh.id='resource-hud';rh.className='glass';rh.innerHTML='<span>WOOD <b id="res-wood">0</b></span><span>STONE <b id="res-stone">0</b></span><span>METAL <b id="res-metal">0</b></span>';hud.appendChild(rh);
 const overlay=document.createElement('section');overlay.id='inventory-overlay';overlay.className='hidden';overlay.innerHTML=`<div class="inventory-shell"><header><div><small>VAST: DEAD ZONE · v0.4</small><h2>INVENTARIO</h2></div><div class="inv-meta"><span id="inv-slots">0/10 HUECOS</span><button id="inventory-close">✕</button></div></header><div class="inventory-body"><aside class="equipment-panel"><small>EQUIPO</small><div class="equip-card"><span>MOCHILA</span><b id="equip-bag">SIN MOCHILA</b></div><div class="equip-card"><span>ARMA</span><b id="equip-primary">M9</b></div><div class="equip-card"><span>CHALECO</span><b id="equip-vest">NINGUNO</b></div><div class="equip-card"><span>CASCO</span><b id="equip-helmet">NINGUNO</b></div><div class="equip-card"><span>ACCESORIOS</span><b id="equip-attach">NINGUNO</b></div><div class="condition-card"><span>ESTADO</span><b id="equip-condition">ESTABLE</b></div></aside><main class="backpack-panel"><div class="section-title"><span>MOCHILA</span><small>Consumibles y munición</small></div><div id="inventory-grid" class="inventory-grid"></div></main><aside class="vicinity-panel"><div class="section-title"><span id="vicinity-title">CERCA</span><small id="vicinity-sub">Abre una caja o armario</small></div><div id="vicinity-grid" class="vicinity-grid"></div></aside></div><footer><span>El mundo se pausa mientras gestionas tu equipo.</span><button id="inventory-close-2">VOLVER A LA ZONA</button></footer></div>`;document.body.appendChild(overlay);invBtn.addEventListener('pointerdown',e=>{e.stopPropagation();openInventory(null)});$('inventory-close').addEventListener('click',closeInventory);$('inventory-close-2').addEventListener('click',closeInventory);renderInventory();const buildOverlay=document.createElement('section');buildOverlay.id='build-overlay';buildOverlay.className='hidden';buildOverlay.innerHTML=`<div class="build-shell"><header><small>CONSTRUCCIÓN</small><h2>REFUGIO</h2><button id="build-close">✕</button></header><div id="build-grid" class="build-grid"></div><footer>Recolecta madera, piedra y metal usando <b>USAR</b> junto a los nodos.</footer></div>`;document.body.appendChild(buildOverlay);const placement=document.createElement('div');placement.id='build-placement';placement.className='hidden';placement.innerHTML='<span id="build-hint">Mueve la cámara para colocar</span><button id="build-place">COLOCAR</button><button id="build-cancel">CANCELAR</button>';hud.appendChild(placement);buildBtn.addEventListener('pointerdown',e=>{e.stopPropagation();openBuildMenu()});$('build-close').onclick=closeBuildMenu;$('build-place').onclick=placeBuild;$('build-cancel').onclick=cancelBuild;updateResourceHUD();
 if(save){$('play-btn').textContent='CONTINUAR EN LA ZONA';const fresh=document.createElement('button');fresh.id='new-game-btn';fresh.textContent='NUEVA PARTIDA';$('play-btn').insertAdjacentElement('afterend',fresh);fresh.addEventListener('click',()=>{if(confirm('¿Borrar la partida guardada y empezar de cero?')){localStorage.removeItem(SAVE_KEY);location.reload()}})}
 // Existing HTML only has three weapon slots. Add three compact slots without touching item hotkeys.
 const hot=$('hotbar');for(let i=3;i<W.length;i++){const b=document.createElement('button');b.dataset.slot=i;b.className='slot locked extra-weapon';b.innerHTML=`<span>${i+1}</span><b>${W[i].label}</b><small>BLOQ.</small>`;hot.insertBefore(b,hot.querySelector('.slot.item'));b.addEventListener('p