ombie=data;m.userData.hitPart=m===head||m===jaw?'head':'body';shootables.push(m)});scene.add(g);zombies.push(data);return data}

function collidesAt(x,z,r=.42){if(x<-WORLD+r||x>WORLD-r||z<-WORLD+r||z>WORLD-r)return true;for(const o of obstacles)if(o.active&&x+r>o.minX&&x-r<o.maxX&&z+r>o.minZ&&z-r<o.maxZ)return true;return false}
function clearLine(ax,az,bx,bz){const steps=Math.ceil(Math.hypot(bx-ax,bz-az)/1.4);for(let i=1;i<steps;i++){const t=i/steps,x=lerp(ax,bx,t),z=lerp(az,bz,t);for(const o of obstacles){if(!o.active||o.tag==='car'||o.tag==='container')continue;if(x>o.minX&&x<o.maxX&&z>o.minZ&&z<o.maxZ)return false}}return true}
let stepTimer=0;
function updatePlayer(dt){const joy=touchMove;let f=(keys.has('KeyW')?1:0)-(keys.has('KeyS')?1:0)+joy.y,r=(keys.has('KeyD')?1:0)-(keys.has('KeyA')?1:0)+joy.x,l=Math.hypot(f,r);if(l>1){f/=l;r/=l}const sprint=(runHeld||keys.has('ShiftLeft'))&&f>.2&&player.stamina>1,speed=sprint?7.1:4.55;if(sprint)player.stamina=clamp(player.stamina-dt*18,0,100);else player.stamina=clamp(player.stamina+dt*11,0,100);const s=Math.sin(yaw),c=Math.cos(yaw),dx=(r*c-f*s)*speed*dt,dz=(-r*s-f*c)*speed*dt;if(!collidesAt(player.pos.x+dx,player.pos.z))player.pos.x+=dx;if(!collidesAt(player.pos.x,player.pos.z+dz))player.pos.z+=dz;if(jumpQueued&&grounded){vy=5.1;grounded=false;jumpQueued=false}vy-=12.8*dt;player.pos.y+=vy*dt;if(player.pos.y<=1.68){player.pos.y=1.68;vy=0;grounded=true}const moving=Math.abs(f)+Math.abs(r)>.13,bob=moving&&grounded?Math.sin(runTime*(sprint?13:9))*.025:0;stepTimer-=dt;if(moving&&grounded&&stepTimer<=0){AudioFX.step(sprint);stepTimer=sprint?.28:.42}shake=Math.max(0,shake-dt*7);yawNode.position.set(player.pos.x,player.pos.y+bob+rand(-shake,shake)*.02,player.pos.z);yawNode.rotation.y=yaw;pitchNode.rotation.x=pitch+rand(-shake,shake)*.008;const fov=aimHeld?settings.fov-13:settings.fov;camera.fov=lerp(camera.fov,fov,clamp(dt*10,0,1));camera.updateProjectionMatrix();recoil=lerp(recoil,0,clamp(dt*12,0,1));recoilSide=lerp(recoilSide,0,clamp(dt*15,0,1));weaponRig.rotation.x=-recoil;weaponRig.rotation.y=recoilSide;weaponRig.position.y=lerp(weaponRig.position.y,-.19+(moving?Math.sin(runTime*8)*.008:0),clamp(dt*9,0,1));player.hunger=clamp(player.hunger-dt*.055,0,100);player.thirst=clamp(player.thirst-dt*.08,0,100);if(player.hunger<=0||player.thirst<=0)damagePlayer(dt*2,false);if(player.bleeding>0)damagePlayer(dt*(.28+.18*player.bleeding),false);noise=clamp(noise-dt*.16,0,1);if(sprint)noise=clamp(noise+dt*.045,0,1);if(reloadUntil&&now()>=reloadUntil)finishReload();if(fireHeld&&W[player.weapon].auto)shoot();updateZone()}

function updateZombies(dt){for(const z of zombies){if(!z.alive){z.deathT+=dt;z.group.rotation.z=lerp(z.group.rotation.z,Math.PI*.48,clamp(dt*4,0,1));z.group.position.y=Math.max(-.7,z.group.position.y-dt*.13);if(z.deathT>7&&!z.group.userData.removed){z.group.userData.removed=true;scene.remove(z.group)}continue}const dx=player.pos.x-z.group.position.x,dz=player.pos.z-z.group.position.z,d=Math.hypot(dx,dz),aTo=Math.atan2(dx,dz),front=Math.abs((((aTo-z.group.rotation.y)+Math.PI)%(Math.PI*2))-Math.PI),sees=d<(z.type==='runner'?28:34)&&front<1.15&&clearLine(z.group.position.x,z.group.position.z,player.pos.x,player.pos.z),hears=d<10+noise*48;if(sees){z.state='chase';z.lastSeen={x:player.pos.x,z:player.pos.z};z.searchUntil=runTime+6}else if(hears){z.state='investigate';z.lastSeen={x:player.pos.x,z:player.pos.z};z.searchUntil=runTime+5}else if(runTime>z.searchUntil&&z.state!=='wander'){z.state='wander';z.lastSeen=null}let ang=z.wanderAngle,speed=z.speed*.28;if(z.state==='chase'||z.state==='investigate'){const target=z.state==='chase'?player.pos:z.lastSeen;ang=Math.atan2(target.x-z.group.position.x,target.z-z.group.position.z);speed=z.speed*(z.state==='chase'?1:.65);if(z.lastSeen&&Math.hypot(z.lastSeen.x-z.group.position.x,z.lastSeen.z-z.group.position.z)<1.3&&z.state==='investigate'){z.state='wander';z.lastSeen=null}}else if(runTime>z.wanderUntil){z.wanderUntil=runTime+rand(2,5);z.wanderAngle+=rand(-1.3,1.3)}if(d<1.28&&sees){speed=0;if(runTime>z.nextAttack){z.nextAttack=runTime+rand(.9,1.25);damagePlayer(rand(z.type==='military'?10:7,z.type==='runner'?13:11),true);AudioFX.groan(1)}}const mx=Math.sin(ang)*speed*dt,mz=Math.cos(ang)*speed*dt;if(!collidesAt(z.group.position.x+mx,z.group.position.z+mz,.34)){z.group.position.x+=mx;z.group.position.z+=mz}else z.wanderAngle+=1.2;z.group.rotation.y=lerpAngle(z.group.rotation.y,ang,clamp(dt*5,0,1));const walk=Math.sin(runTime*7*z.speed)*.55*(speed>0?.8:0);z.limbs[0].rotation.x=walk;z.limbs[1].rotation.x=-walk;z.limbs[2].rotation.x=-walk;z.limbs[3].rotation.x=walk;z.groanAt-=dt;if(z.groanAt<=0&&d<22){AudioFX.groan(clamp(1-d/24,.2,1));z.groanAt=rand(3.5,9)}}}
function lerpAngle(a,b,t){let d=((b-a+Math.PI)%(Math.PI*2))-Math.PI;return a+d*t}

function shoot(){if(!started||paused||dead)return;const w=W[player.weapon],n=now();if(n<nextShot||reloadUntil)return;if(w.mag<=0){AudioFX.click();toast('Sin munición · RECARGA','bad');nextShot=n+.3;return}w.mag--;nextShot=n+w.rate;const suppressed=player.attachments.suppressor&&['m9','mp5','m4'].includes(w.id);noise=clamp(noise+w.noise*(suppressed?.28:1),0,1);recoil=Math.min(.16,recoil+(w.id==='shotgun'?.1:w.id==='mosin'?.12:w.id==='ak'?.055:w.auto?.035:.05));recoilSide+=rand(-.018,.018);pitch=clamp(pitch-rand(.004,.013)*(w.id==='ak'?1.4:1),-1.22,1.22);shake=Math.min(.7,shake+(w.id==='shotgun'||w.id==='mosin'?.42:.18));AudioFX.gun(w);weaponRig.userData.muzzle.intensity=suppressed?1.2:4.5;setTimeout(()=>{if(weaponRig?.userData?.muzzle)weaponRig.userData.muzzle.intensity=0},35);let hit=false,kill=false;for(let p=0;p<(w.pellets||1);p++){const origin=new THREE.Vector3();camera.getWorldPosition(origin);const q=camera.getWorldQuaternion(new THREE.Quaternion()),dir=new THREE.Vector3(0,0,-1).applyQuaternion(q),rv=new THREE.Vector3(1,0,0).applyQuaternion(q),uv=new THREE.Vector3(0,1,0).applyQuaternion(q),spread=w.spread*(aimHeld?(player.attachments.reddot?.42:.58):1);dir.addScaledVector(rv,rand(-spread,spread)).addScaledVector(uv,rand(-spread,spread)).normalize();ray.set(origin,dir);ray.far=w.range;const hits=ray.intersectObjects(shootables.filter(m=>m.userData.zombie?.alive),false);if(hits.length){const h=hits[0],z=h.object.userData.zombie,head=h.object.userData.hitPart==='head';z.hp-=w.damage*(head?w.head:1)*rand(.92,1.08);z.state='chase';z.lastSeen={x:player.pos.x,z:player.pos.z};z.searchUntil=runTime+8;hit=true;if(z.hp<=0){killZombie(z,head);kill=true}}}if(hit)showHitmarker(kill);updateAmmoUI()}
function killZombie(z,head=false){if(!z.alive)return;z.alive=false;player.kills++;toast(head?'TIRO A LA CABEZA +1':'Zombi abatido +1','good');const chance=z.type==='military'?.72:.35;if(Math.random()<chance){const table=z.type==='military'?['ammo556','ammo762','bandage','medkit','helmet']:['ammo9','food','water','bandage'];spawnLoose(pick(table),z.group.position.x+rand(-.5,.5),z.group.position.z+rand(-.5,.5),`drop-${Date.now()}-${Math.random()}`)}if(player.kills===5&&currentObjective<4)setObjective(4)}
function reload(){const w=W[player.weapon],reserve=player.inventory[w.ammo]||0;if(reloadUntil||w.mag>=w.magSize||reserve<=0)return;reloadUntil=now()+w.reload;toast('Recargando…');updateAmmoUI()}
function finishReload(){const w=W[player.weapon],need=w.magSize-w.mag,take=Math.min(need,player.inventory[w.ammo]||0);w.mag+=take;player.inventory[w.ammo]-=take;reloadUntil=0;updateAmmoUI();renderInventory()}
function switchWeapon(i){const w=W[i];if(!w?.unlocked){toast('Aún