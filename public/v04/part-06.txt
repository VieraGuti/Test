 no tienes esa arma','bad');return}player.weapon=i;reloadUntil=0;buildWeapon();updateWeaponSlots();updateAmmoUI();saveGame()}
function damagePlayer(amount,flash=true){if(dead)return;let mult=1;if(player.armor.vest)mult*=.75;if(player.armor.helmet)mult*=.9;player.hp=clamp(player.hp-amount*mult,0,100);if(flash){shake=.6;$('damage-vignette').style.opacity='.9';setTimeout(()=>$('damage-vignette').style.opacity='0',180);if(Math.random()<.16&&!player.bleeding){player.bleeding=1;toast('ESTÁS SANGRANDO','bad')}}if(player.hp<=0)die()}

function slotsUsed(){let n=0;for(const [t,c] of Object.entries(player.inventory)){if(!c||!ITEMS[t])continue;n+=Math.ceil(c/ITEMS[t].stack)}return n}
function capacity(){return BAG_CAP[player.bagLevel]}
function addItem(type,count=1){const def=ITEMS[type];if(!def)return false;if(def.kind==='gear'&&type.startsWith('backpack')){player.bagLevel=Math.max(player.bagLevel,type==='backpack_large'?2:1);toast(`${def.name} equipada`,'good');return true}if(def.kind==='armor'){player.armor[type]=true;toast(`${def.name} equipado`,'good');return true}if(def.kind==='attachment'){player.attachments[type]=true;toast(`${def.name} equipado`,'good');buildWeapon();return true}if(def.kind==='weapon'){const wi=def.weapon;W[wi].unlocked=true;if(W[wi].mag===0){const load=Math.min(W[wi].magSize,player.inventory[W[wi].ammo]||0);W[wi].mag=load;player.inventory[W[wi].ammo]=(player.inventory[W[wi].ammo]||0)-load}toast(`${def.name} conseguida`,'good');updateWeaponSlots();return true}const current=player.inventory[type]||0,before=Math.ceil(current/def.stack),after=Math.ceil((current+count)/def.stack);if(slotsUsed()-before+after>capacity()){toast('No tienes espacio en la mochila','bad');return false}player.inventory[type]=current+count;return true}
function useItem(type){if((player.inventory[type]||0)<=0)return;if(type==='medkit'){if(player.hp>95){toast('Tu vida está casi llena');return}player.hp=clamp(player.hp+55,0,100)}else if(type==='bandage'){if(!player.bleeding){toast('No estás sangrando');return}player.bleeding=0}else if(type==='food')player.hunger=clamp(player.hunger+40,0,100);else if(type==='water')player.thirst=clamp(player.thirst+50,0,100);else return;player.inventory[type]--;AudioFX.pickup();toast(`${ITEMS[type].name} usado`,'good');renderInventory();updateUI(true);saveGame()}

function updateInteract(dt){interactTarget=null;let nearest=2.35;for(const l of loots){if(l.picked)continue;l.group.position.y=.34+Math.sin(runTime*2+l.phase)*.045;l.ring.rotation.z+=dt*.65;const d=dist(player.pos,l.group.position);if(d<nearest){nearest=d;interactTarget=l}}for(const c of containers){const d=dist(player.pos,c);if(d<nearest){nearest=d;interactTarget=c}}for(const d of doors){const dd=dist(player.pos,d);if(dd<nearest){nearest=dd;interactTarget=d}}for(const n of resourceNodes){const dd=Math.hypot(player.pos.x-n.x,player.pos.z-n.z);if(dd<nearest){nearest=dd;interactTarget=n}}if(interactTarget){$('interact-prompt').classList.remove('hidden');$('interact-text').textContent=interactLabel(interactTarget)}else $('interact-prompt').classList.add('hidden')}
function interactLabel(t){if(t.type==='door')return t.open?'Cerrar puerta':'Abrir puerta';if(t.type==='container')return `Registrar ${t.label}`;if(t.type==='loot')return `Recoger ${ITEMS[t.itemType]?.name||'objeto'}`;if(t.type==='resource')return t.resource==='wood'?'Talar árbol':t.resource==='metal'?'Picar metal':'Picar piedra';return 'Usar'}
function interact(){const t=interactTarget;if(!t)return;if(t.type==='door'){toggleDoor(t);return}if(t.type==='container'){openInventory(t);return}if(t.type==='resource'){harvestResource(t);return}if(t.type==='loot'){if(!addItem(t.itemType,t.count))return;t.picked=true;pickedLoot.add(t.id);scene.remove(t.group);AudioFX.pickup();toast(`${ITEMS[t.itemType].name}${t.count>1?' +'+t.count:''}`,'good');if(currentObjective===0&&t.id.startsWith('gas-'))setObjective(1);renderInventory();saveGame()}}

function setObjective(i){currentObjective=i;const O=[['Registra la gasolinera','Busca comida, agua y una mochila'],['Visita la clínica','Consigue suministros médicos'],['Saquea la comisaría','Busca un arma automática'],['Infiltra la base militar','El mejor equipo está al sureste'],['Sobrevive a la zona','Explora y espera eventos dinámicos'],['HELICÓPTERO CAÍDO','Localiza el humo y recupera el cargamento']];const o=O[i]||O[4];$('objective-text').textContent=o[0];$('objective-sub').textContent=o[1];updateBeacons();toast('OBJETIVO ACTUALIZADO');saveGame()}
function updateObjectives(){if(currentObjective===0&&Math.hypot(player.pos.x+48,player.pos.z-20)<15)setObjective(1);else if(currentObjective===1&&Math.hypot(player.pos.x-50,player.pos.z-48)<17)setObjective(2);else if(currentObjective===2&&Math.hypot(player.pos.x+72,player.pos.z+48)<18)setObjective(3);else if(currentObjective===3&&Math.hypot(player.pos.x-78,player.pos.z+72)<27)setObjective(4)}
function updateZone(){zoneCooldown=Math.max(0,zoneCooldown-.016);const zones=[['GASOLINERA',-48,20,24],['CLÍNICA',50,48,28],['COMISARÍA',-72,-48,26],['GRANJA',28,-72,26],['BASE MILITAR',78,-72,36]],hit=zones.find(z=>Math.hypot(player.pos.x-z[1],player.pos.z-z[2])<z[3]);const n=hit?.[0]||'';if(n&&n!==currentZone&&zoneCooldown<=0){currentZone=n;zoneCooldown=3;zoneBanner(n)}else if(!n)currentZone='';updateObjectives()}
function zoneBanner(name){let el=$('zone-banner');if(!el){el=document.createElement('div');el.id='zone-banner';document.body.appendChild(el)}el.textContent=name;el.classList.add('show');clearTimeout(zoneBanner.t);zoneBanner.t=setTimeout(()=>el.classList.remove('show'),1800)}

function triggerHeliEvent(){if(heliEvent?.active)return;let x=rand(-115,115),z=rand(-115,115);if(Math.abs(x)<35&&Math.abs(z)<35){x+=70;z-=55}heliEvent={active:true,x,z,looted:false};buildHeliCrash(x,z);setObjective(5);toast('¡SE HA ESTRELLADO UN HELICÓPTERO!','bad');saveGame()}
function restoreHeliEvent(){if(heliEvent?.active)buildHeliCrash(heliEvent.x,heliEvent.z)}
function buildHeliCrash(x,z){const g=new THREE.Group(),m=new THREE.MeshStandardMaterial({color:0x252b28,roughness:.72,metalness:.25});const body=new THREE.Mesh(new THREE.CapsuleGeometry(.65,2.4,4,8),m);body.rotation.z=Math.PI/2;body.position.y=.85;const tail=new THREE.Mesh(new THREE.BoxGeometry(3,.18,.18),m);tail.position.set(-2.4,.95,0);g.add(body,tail);g.position.set(x,0,z);g.rotation.y=rand(0,Math.PI*2);scene.add(g);const smokeMat=new THREE.MeshBasicMaterial({color:0x1d211f,transparent:true,opacity:.28,depthWrite:false});for(let i=0;i<7;i++){const s=new THREE.Mesh(new THREE.SphereGeometry(.8+i*.16,8,6),smokeMat);s.position.set(x+rand(-.5,.5),2+i*1.4,z+rand(-.5,.5));scene.add(s)}spawnContainer('heli-crate','Cargamento del helicóptero',x+2,z+1,{ak:1,mosin:1,ammo762:60,medkit:2,vest:1,helmet:1,suppressor:1,reddot:1});for(let i=0;i<5;i++)spawnZombie(new THREE.Vector3(x+rand(-8,8),0,z+rand(-8,8)),'military');const b=new THREE.Mesh(new THREE.CylinderGeometry(.05,.8,22,12,1,true),new THREE.MeshBasicMaterial({color:0xaa4e2f,transparent:true,opacity:.17,depthWrite:false}));b.position.set(x,11,z);b.userData.objective=5;scene.add(b);beacons.push(b);updateBeacons()}

function updateWorldTime(){const sim=8+runTime/300*24,day=Math.floor(sim/24)+1,hour=sim%24,light=clamp(Math.sin((hour-6)/24*Math.PI*2)*1.8+.15,0,1);sun.intensity=.08+light*2.2;hemi.intensity=.18+light*1.15;renderer.toneMappingExposure=.53+light*.43;tmpColor.copy(new THREE.Color(0x0b1116)).lerp(new THREE.Color(0x929b95),light);scene.background.copy(tmpColor);scene.f