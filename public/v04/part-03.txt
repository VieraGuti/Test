d,h,mat)}
function addDoor(id,x,z,width,height,axis='x',swing=1){const pivot=new THREE.Group();pivot.position.set(axis==='x'?x-width/2:x,0,axis==='z'?z-width/2:z);scene.add(pivot);const mesh=new THREE.Mesh(new THREE.BoxGeometry(width,height,.12),new THREE.MeshStandardMaterial({color:0x403d36,roughness:.82,metalness:.15}));mesh.position.set(width/2,height/2,0);pivot.add(mesh);if(axis==='z')pivot.rotation.y=Math.PI/2;const obs=axis==='x'?addObstacle(x-width/2,x+width/2,z-.2,z+.2,'door'):addObstacle(x-.2,x+.2,z-width/2,z+width/2,'door');const d={type:'door',id,pivot,mesh,obs,x,z,axis,open:false,swing};doors.push(d);return d}
function toggleDoor(d){if(d.open&&dist(player.pos,d)<1.4){toast('Apártate para cerrar','bad');return}d.open=!d.open;d.obs.active=!d.open;d.pivot.rotation.y=(d.axis==='z'?Math.PI/2:0)+(d.open?d.swing*Math.PI/2:0);AudioFX.click()}
function addShelf(x,z,w=5,d=1){const m=new THREE.MeshStandardMaterial({color:0x4b453c,roughness:.95});addBlock(x,z,w,d,.2,m,true,.6);addBlock(x,z,w,d,.18,m,false,1.6)}
function addCar(x,z,r=0){const g=new THREE.Group();g.position.set(x,0,z);g.rotation.y=r;const m=new THREE.MeshStandardMaterial({color:pick([0x555d58,0x75554a,0x4a555f]),roughness:.72,metalness:.28});const a=new THREE.Mesh(new THREE.BoxGeometry(3.3,.65,1.55),m);a.position.y=.65;const b=new THREE.Mesh(new THREE.BoxGeometry(1.9,.62,1.42),m);b.position.set(-.25,1.22,0);g.add(a,b);scene.add(g);addObstacle(x-1.85,x+1.85,z-1.35,z+1.35,'car')}
function addDeadTree(x,z,s){const g=new THREE.Group(),m=new THREE.MeshStandardMaterial({color:0x504638,roughness:1});const t=new THREE.Mesh(new THREE.CylinderGeometry(.14*s,.24*s,3.8*s,7),m);t.position.y=1.9*s;g.add(t);for(let i=0;i<3;i++){const b=new THREE.Mesh(new THREE.CylinderGeometry(.04*s,.08*s,1.7*s,6),m);b.position.set(rand(-.35,.35),rand(2.4,3.3)*s,0);b.rotation.z=rand(-1,1);g.add(b)}g.position.set(x,0,z);scene.add(g)}

function addResourceTree(x,z,s,id){if(pickedLoot.has(`res-${id}`))return;const g=new THREE.Group(),trunkMat=new THREE.MeshStandardMaterial({color:0x514638,roughness:1}),leafMat=new THREE.MeshStandardMaterial({color:0x394638,roughness:1});const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.18*s,.3*s,4.1*s,8),trunkMat);trunk.position.y=2.05*s;g.add(trunk);for(let i=0;i<3;i++){const b=new THREE.Mesh(new THREE.CylinderGeometry(.05*s,.1*s,1.6*s,6),trunkMat);b.position.set(rand(-.3,.3),rand(2.6,3.5)*s,0);b.rotation.z=rand(-.9,.9);g.add(b)}const crown=new THREE.Mesh(new THREE.DodecahedronGeometry(1.05*s,0),leafMat);crown.position.y=4.05*s;crown.scale.set(1,1.25,1);g.add(crown);g.position.set(x,0,z);scene.add(g);const obs=addObstacle(x-.35*s,x+.35*s,z-.35*s,z+.35*s,'resource');resourceNodes.push({type:'resource',resource:'wood',id,x,z,group:g,hp:4,maxHp:4,yield:9,obs})}
function addResourceRock(x,z,s,type,id){if(pickedLoot.has(`res-${id}`))return;const color=type==='metal'?0x555b57:0x676761,g=new THREE.Group(),mat=new THREE.MeshStandardMaterial({color,roughness:.9,metalness:type==='metal'?.28:.02});for(let i=0;i<3;i++){const r=new THREE.Mesh(new THREE.DodecahedronGeometry(rand(.45,.75)*s,0),mat);r.position.set(rand(-.45,.45)*s,rand(.35,.6)*s,rand(-.35,.35)*s);r.rotation.set(rand(0,1),rand(0,1),rand(0,1));g.add(r)}g.position.set(x,0,z);scene.add(g);const obs=addObstacle(x-.7*s,x+.7*s,z-.7*s,z+.7*s,'resource');resourceNodes.push({type:'resource',resource:type,id,x,z,group:g,hp:type==='metal'?6:5,maxHp:type==='metal'?6:5,yield:type==='metal'?5:7,obs})}
function harvestResource(n){if(!n||harvestLock>now())return;harvestLock=now()+.42;const amount=n.yield;if(!addItem(n.resource,amount))return;n.hp--;noise=clamp(noise+.1,0,1);AudioFX.burst?.(.12,.08,n.resource==='wood'?650:1100,110);toast(`${ITEMS[n.resource].name} +${amount}`,'good');const base=n.group.scale.x;n.group.scale.set(base*.93,base*1.03,base*.93);setTimeout(()=>{if(n.group.parent)n.group.scale.set(base,base,base)},110);if(n.hp<=0){scene.remove(n.group);n.obs.active=false;pickedLoot.add(`res-${n.id}`);resourceNodes.splice(resourceNodes.indexOf(n),1);toast(n.resource==='wood'?'Árbol talado':'Nodo agotado','good')}updateResourceHUD();renderInventory();saveGame()}

const BUILD_RECIPES={
 barricade:{label:'BARRICADA',cost:{wood:25},w:2.8,d:.35,h:1.25},
 wall:{label:'MURO',cost:{wood:45,stone:10},w:3,d:.35,h:2.25},
 campfire:{label:'FOGATA',cost:{wood:20,stone:12},w:1.1,d:1.1,h:.45},
 stash:{label:'CAJA',cost:{wood:35,metal:5},w:1.2,d:.8,h:.72}
};
function canAfford(cost){return Object.entries(cost).every(([k,v])=>(player.inventory[k]||0)>=v)}
function payCost(cost){for(const [k,v] of Object.entries(cost))player.inventory[k]=Math.max(0,(player.inventory[k]||0)-v)}
function openBuildMenu(){if(dead||!started||inventoryOpen)return;paused=true;fireHeld=false;$('build-overlay').classList.remove('hidden');renderBuildMenu()}
function closeBuildMenu(){paused=false;$('build-overlay').classList.add('hidden');clock.getDelta()}
function renderBuildMenu(){const g=$('build-grid');if(!g)return;g.innerHTML='';for(const [type,r] of Object.entries(BUILD_RECIPES)){const b=document.createElement('button');b.className='build-card'+(canAfford(r.cost)?'':' disabled');b.innerHTML=`<b>${r.label}</b><span>${Object.entries(r.cost).map(([k,v])=>`${ITEMS[k].short} ${v}`).join(' · ')}</span>`;b.onclick=()=>{if(!canAfford(r.cost)){toast('Faltan recursos','bad');return}selectBuild(type)};g.appendChild(b)}}
function selectBuild(type){buildMode=type;closeBuildMenu();$('build-placement').classList.remove('hidden');createBuildPreview(type)}
function createBuildPreview(type){if(buildPreview){scene.remove(buildPreview);buildPreview=null}const r=BUILD_RECIPES[type],mat=new THREE.MeshBasicMaterial({color:0x5bbf6a,transparent:true,opacity:.35,depthWrite:false});if(type==='campfire'){buildPreview=new THREE.Mesh(new THREE.CylinderGeometry(.55,.55,.22,12),mat)}else buildPreview=new THREE.Mesh(new THREE.BoxGeometry(r.w,r.h,r.d),mat);scene.add(buildPreview)}
function updateBuildPreview(){if(!buildMode||!buildPreview||paused)return;const dir=new THREE.Vector3(0,0,-1).applyQuaternion(camera.getWorldQuaternion(new THREE.Quaternion()));dir.y=0;dir.normalize();const d=3.4,x=player.pos.x+dir.x*d,z=player.pos.z+dir.z*d,r=BUILD_RECIPES[buildMode];buildPreview.position.set(x,r.h/2,z);buildPreview.rotation.y=yaw;const radius=Math.max(r.w,r.d)*.52;buildValid=canAfford(r.cost)&&!collidesAt(x,z,radius)&&Math.abs(x)<WORLD-3&&Math.abs(z)<WORLD-3;buildPreview.material.color.set(buildValid?0x58c76a:0xc34b3e);$('build-place').classList.toggle('invalid',!buildValid)}
function placeBuild(){if(!buildMode||!buildValid){toast('No puedes construir aquí','bad');return}const r=BUILD_RECIPES[buildMode],x=buildPreview.position.x,z=buildPreview.position.z,rot=buildPreview.rotation.y,id=`b-${Date.now()}-${Math.floor(Math.random()*9999)}`;payCost(r.cost);scene.remove(buildPreview);buildPreview=null;spawnStructure(buildMode,x,z,rot,id);toast(`${r.label} colocada`,'good');updateResourceHUD();renderInventory();saveGame();cancelBuild()}
function cancelBuild(){if(buildPreview)scene.remove(buildPreview);buildPreview=null;buildMode=null;buildValid=false;$('build-placement')?.classList.add('hidden')}
function spawnStructure(type,x,z,rot=0,id=`b-${Date.now()}`){const r=BUILD_RECIPES[type];if(!r)return;const g=new THREE.Group();g.position.set(x,0,z);g.rotation.y=rot;let obstacle=null;if(type==='campfire'){const stoneMat=new THREE.MeshStandardMaterial({color:0x65645c,roughness:1});for(let i=0;i<8;i++){const a=i/8*Math.PI*2,q=new THREE.Me