sh(new THREE.DodecahedronGeometry(.13,0),stoneMat);q.position.set(Math.cos(a)*.43,.12,Math.sin(a)*.43);g.add(q)}const ember=new THREE.Mesh(new THREE.CylinderGeometry(.26,.3,.12,10),new THREE.MeshBasicMaterial({color:0xb14f27}));ember.position.y=.1;g.add(ember);const light=new THREE.PointLight(0xff7d3a,1.05,7,2);light.position.y=.55;g.add(light)}else{const mat=new THREE.MeshStandardMaterial({color:type==='stash'?0x4b4437:0x70553d,roughness:.94});const mesh=new THREE.Mesh(new THREE.BoxGeometry(r.w,r.h,r.d),mat);mesh.position.y=r.h/2;mesh.castShadow=true;g.add(mesh)}scene.add(g);if(type!=='campfire'){const rr=Math.max(r.w,r.d)*.48;obstacle=addObstacle(x-rr,x+rr,z-rr,z+rr,'build')}const st={id,type,x,z,rot,group:g,obs:obstacle,preview:false};structures.push(st);if(type==='stash'){const c={type:'container',id:`stash-${id}`,label:'Caja construida',x,z,group:g,lid:null,items:Object.assign({},containerSave[`stash-${id}`]||{})};containers.push(c)}return st}
function restoreStructures(){for(const s of save?.structures||[])spawnStructure(s.type,s.x,s.z,s.rot,s.id)}
function updateCampfires(dt){for(const s of structures){if(s.type!=='campfire')continue;if(Math.hypot(player.pos.x-s.x,player.pos.z-s.z)<3.2&&player.hp<100&&player.hunger>10)player.hp=clamp(player.hp+dt*.35,0,100)}}
function updateResourceHUD(){if(!$('resource-hud'))return;$('res-wood').textContent=player.inventory.wood||0;$('res-stone').textContent=player.inventory.stone||0;$('res-metal').textContent=player.inventory.metal||0}

function addLamp(x,z){const g=new THREE.Group(),m=new THREE.MeshStandardMaterial({color:0x343a37,metalness:.55,roughness:.6});const p=new THREE.Mesh(new THREE.CylinderGeometry(.07,.1,5.2,8),m);p.position.y=2.6;g.add(p);g.position.set(x,0,z);scene.add(g);if(lights.length<12){const l=new THREE.PointLight(0xffa95e,0,13,2);l.position.set(x,4.8,z);scene.add(l);lights.push(l)}}
function createBeacon(x,z,obj){const beam=new THREE.Mesh(new THREE.CylinderGeometry(.08,1.15,16,14,1,true),new THREE.MeshBasicMaterial({color:0xd87537,transparent:true,opacity:.2,depthWrite:false}));beam.position.set(x,8,z);beam.userData.objective=obj;scene.add(beam);beacons.push(beam);updateBeacons()}
function updateBeacons(){beacons.forEach(b=>b.visible=b.userData.objective===currentObjective)}

function spawnLoose(type,x,z,id,count=null){if(pickedLoot.has(id)||!ITEMS[type])return;const def=ITEMS[type],c={medical:0xcbd4ca,food:0xb27a42,drink:0x4f96ad,ammo:0xb7a36a,gear:0x566757,armor:0x58625c,attachment:0x6c6f64,weapon:0x725d49}[def.kind]||0xffffff;const g=new THREE.Group();g.position.set(x,.35,z);const mat=new THREE.MeshStandardMaterial({color:c,emissive:c,emissiveIntensity:.14,roughness:.6,metalness:def.kind==='ammo'?.35:.08});let mesh;if(def.kind==='weapon')mesh=new THREE.Mesh(new THREE.BoxGeometry(.12,.12,.88),mat);else if(def.kind==='gear'||def.kind==='armor')mesh=new THREE.Mesh(new THREE.BoxGeometry(.45,.5,.25),mat);else if(def.kind==='drink')mesh=new THREE.Mesh(new THREE.CylinderGeometry(.11,.14,.42,9),mat);else mesh=new THREE.Mesh(new THREE.BoxGeometry(.36,.24,.22),mat);const ring=new THREE.Mesh(new THREE.TorusGeometry(.34,.018,5,18),new THREE.MeshBasicMaterial({color:c,transparent:true,opacity:.5}));ring.rotation.x=Math.PI/2;ring.position.y=-.22;g.add(mesh,ring);scene.add(g);loots.push({type:'loot',itemType:type,count:count??(def.kind==='ammo'?Math.min(def.stack,type==='ammo9'?18:type==='ammo556'?24:12):1),id,group:g,ring,phase:Math.random()*6.28,picked:false})}
function spawnContainer(id,label,x,z,defaults){const items=Object.assign({},containerSave[id]??defaults),g=new THREE.Group();g.position.set(x,.38,z);const base=new THREE.Mesh(new THREE.BoxGeometry(1.05,.65,.72),new THREE.MeshStandardMaterial({color:0x4c514b,roughness:.82,metalness:.25})),lid=new THREE.Mesh(new THREE.BoxGeometry(1.08,.13,.75),new THREE.MeshStandardMaterial({color:0x62675e,roughness:.78,metalness:.25}));lid.position.y=.39;g.add(base,lid);scene.add(g);addObstacle(x-.55,x+.55,z-.4,z+.4,'container');containers.push({type:'container',id,label,x,z,group:g,lid,items})}

function buildWeapon(){if(weaponRig)camera.remove(weaponRig);weaponRig=new THREE.Group();camera.add(weaponRig);weaponRig.position.set(.22,-.19,-.42);const w=W[player.weapon],metal=new THREE.MeshStandardMaterial({color:0x282e2d,metalness:.72,roughness:.34}),grip=new THREE.MeshStandardMaterial({color:0x25201d,roughness:.9}),box=(sx,sy,sz,x,y,z,m=metal)=>{const q=new THREE.Mesh(new THREE.BoxGeometry(sx,sy,sz),m);q.position.set(x,y,z);weaponRig.add(q);return q},barrel=(rad,len,z)=>{const q=new THREE.Mesh(new THREE.CylinderGeometry(rad,rad,len,10),metal);q.rotation.x=Math.PI/2;q.position.set(0,.025,z);weaponRig.add(q)};if(w.id==='m9'){box(.115,.105,.34,0,.01,-.08);box(.095,.17,.11,0,-.11,.01,grip).rotation.x=-.16;barrel(.026,.22,-.30)}else if(w.id==='shotgun'){box(.12,.13,.5,0,0,-.12);box(.09,.18,.18,0,-.14,.05,grip);barrel(.036,.72,-.68)}else if(w.id==='mosin'){box(.105,.11,.72,0,.01,-.26,new THREE.MeshStandardMaterial({color:0x5b3e2d,roughness:.86}));barrel(.024,.75,-.82)}else{box(.13,.14,.52,0,.01,-.14);box(.11,.19,.18,0,-.13,.03,grip);barrel(.024,.48,-.62);if(player.attachments.reddot){const sight=box(.055,.07,.09,0,.13,-.18,new THREE.MeshStandardMaterial({color:0x111413,roughness:.55}));sight.material.emissive=new THREE.Color(0x551100);sight.material.emissiveIntensity=.2}}if(player.attachments.suppressor&&['m9','mp5','m4'].includes(w.id))barrel(.04,.22,w.id==='m9'?-.46:-.91);const muzzle=new THREE.PointLight(0xff8a43,0,2.2,2);muzzle.position.set(0,.02,w.id==='shotgun'?-.98:w.id==='mosin'?-.98:-.78);weaponRig.add(muzzle);weaponRig.userData.muzzle=muzzle}

function randomSpawn(min=28,max=80){for(let i=0;i<30;i++){const a=Math.random()*Math.PI*2,d=rand(min,max),x=player.pos.x+Math.cos(a)*d,z=player.pos.z+Math.sin(a)*d;if(!collidesAt(x,z,.7)&&Math.abs(x)<WORLD-4&&Math.abs(z)<WORLD-4)return new THREE.Vector3(x,0,z)}return new THREE.Vector3(rand(-145,145),0,rand(-145,145))}
function spawnZombie(pos,type='walker'){const military=type==='military',runner=type==='runner';const g=new THREE.Group();g.position.copy(pos);const skin=new THREE.MeshStandardMaterial({color:new THREE.Color().setHSL(rand(.18,.28),.22,rand(.28,.38)),roughness:.96}),cloth=new THREE.MeshStandardMaterial({color:military?0x4c5948:pick([0x4d5147,0x51443e,0x3e4a50,0x5b5545]),roughness:1});const torso=new THREE.Mesh(new THREE.CapsuleGeometry(.31,.65,3,7),cloth);torso.position.y=1.12;torso.scale.z=.68;const head=new THREE.Mesh(new THREE.SphereGeometry(.25,9,7),skin);head.position.set(0,1.85,.02);const jaw=new THREE.Mesh(new THREE.BoxGeometry(.22,.12,.19),skin);jaw.position.set(0,1.72,-.11);g.add(torso,head,jaw);const limbs=[];for(const s of [-1,1]){const arm=new THREE.Mesh(new THREE.CapsuleGeometry(.085,.55,2,6),skin);arm.position.set(s*.38,1.2,-.05);const leg=new THREE.Mesh(new THREE.CapsuleGeometry(.105,.7,2,6),cloth);leg.position.set(s*.16,.42,0);g.add(arm,leg);limbs.push(arm,leg)}if(military){const helm=new THREE.Mesh(new THREE.SphereGeometry(.27,8,5,0,Math.PI*2,0,Math.PI*.55),new THREE.MeshStandardMaterial({color:0x354137,roughness:.8}));helm.position.set(0,1.91,0);g.add(helm)}const data={group:g,type,alive:true,hp:military?145:runner?82:rand(85,115),speed:military?1.55:runner?2.65:rand(1.25,1.7),state:'wander',lastSeen:null,searchUntil:0,wanderAngle:Math.random()*Math.PI*2,wanderUntil:0,nextAttack:0,groanAt:rand(2,7),deathT:0,limbs,head,torso};[torso,head,jaw,...limbs].forEach(m=>{m.castShadow=true;m.userData.z