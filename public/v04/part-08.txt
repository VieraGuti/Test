ointerdown',e=>{e.stopPropagation();switchWeapon(i)})}updateWeaponSlots();
}
function updateBagButton(){const e=$('bag-status');if(e)e.textContent=`${slotsUsed()}/${capacity()}`;const a=$('armor-status');if(a)a.textContent=player.armor.vest||player.armor.helmet?'PROTEGIDO':'SIN ARMADURA'}
function renderInventory(){if(!$('inventory-grid'))return;const grid=$('inventory-grid');grid.innerHTML='';for(const [type,count] of Object.entries(player.inventory).filter(([t,n])=>n>0&&ITEMS[t])){const d=ITEMS[type],card=document.createElement('div');card.className='inv-item kind-'+d.kind;card.innerHTML=`<div class="item-code">${d.short}</div><div class="item-copy"><b>${d.name}</b><span>${d.desc}</span></div><strong>x${count}</strong>`;if(['medkit','bandage','food','water'].includes(type)){const use=document.createElement('button');use.textContent='USAR';use.onclick=e=>{e.stopPropagation();useItem(type)};card.appendChild(use)}grid.appendChild(card)}$('inv-slots').textContent=`${slotsUsed()}/${capacity()} HUECOS`;$('equip-bag').textContent=player.bagLevel===2?'MOCHILA MILITAR':player.bagLevel===1?'MOCHILA DE ASALTO':'SIN MOCHILA';$('equip-primary').textContent=W[player.weapon].label;$('equip-vest').textContent=player.armor.vest?'TÁCTICO':'NINGUNO';$('equip-helmet').textContent=player.armor.helmet?'BALÍSTICO':'NINGUNO';const at=[];if(player.attachments.reddot)at.push('PUNTO ROJO');if(player.attachments.suppressor)at.push('SILENCIADOR');$('equip-attach').textContent=at.join(' · ')||'NINGUNO';$('equip-condition').textContent=player.bleeding?`SANGRADO x${player.bleeding}`:'ESTABLE';$('equip-condition').classList.toggle('danger',!!player.bleeding);renderVicinity();updateBagButton();updateAmmoUI()}
function renderVicinity(){const grid=$('vicinity-grid');if(!grid)return;grid.innerHTML='';if(!activeContainer){$('vicinity-title').textContent='CERCA';$('vicinity-sub').textContent='Abre una caja o armario';grid.innerHTML='<div class="empty-vicinity">Nada seleccionado</div>';return}$('vicinity-title').textContent=activeContainer.label.toUpperCase();$('vicinity-sub').textContent='Contenido del contenedor';const entries=Object.entries(activeContainer.items).filter(([t,n])=>n>0&&ITEMS[t]);if(!entries.length){grid.innerHTML='<div class="empty-vicinity">VACÍO</div>';return}for(const [type,count] of entries){const d=ITEMS[type],card=document.createElement('div');card.className='vicinity-item';card.innerHTML=`<div><b>${d.name}</b><span>${d.desc}</span></div><strong>x${count}</strong>`;const take=document.createElement('button');take.textContent='COGER';take.onclick=()=>takeFromContainer(type);card.appendChild(take);grid.appendChild(card)}}
function takeFromContainer(type){if(!activeContainer?.items[type])return;const d=ITEMS[type],amount=d.kind==='ammo'?Math.min(d.stack,activeContainer.items[type]):1;if(!addItem(type,amount))return;activeContainer.items[type]-=amount;if(activeContainer.items[type]<=0)delete activeContainer.items[type];containerSave[activeContainer.id]=Object.assign({},activeContainer.items);AudioFX.pickup();toast(`${d.name} +${amount}`,'good');renderInventory();saveGame();if(activeContainer.id==='heli-crate'&&Object.keys(activeContainer.items).length===0&&currentObjective===5){heliEvent.looted=true;setObjective(4)}}
function openInventory(container=null){if(dead||!started)return;if(buildMode)cancelBuild();activeContainer=container;inventoryOpen=true;paused=true;fireHeld=runHeld=aimHeld=false;$('inventory-overlay').classList.remove('hidden');document.body.classList.add('inventory-open');$('pause').classList.add('hidden');renderInventory();saveGame()}
function closeInventory(){if(!inventoryOpen)return;inventoryOpen=false;activeContainer=null;paused=false;$('inventory-overlay').classList.add('hidden');document.body.classList.remove('inventory-open');clock.getDelta();AudioFX.resume();saveGame()}

function saveGame(){const data={pos:{x:player.pos.x,z:player.pos.z},hp:player.hp,hunger:player.hunger,thirst:player.thirst,stamina:player.stamina,bleeding:player.bleeding,kills:player.kills,bagLevel:player.bagLevel,armor:player.armor,attachments:player.attachments,inventory:player.inventory,weapon:player.weapon,weapons:W.map(w=>({mag:w.mag,unlocked:w.unlocked})),runTime,objective:currentObjective,yaw,pitch,pickedLoot:[...pickedLoot],containers:containerSave,heliEvent,structures:structures.filter(s=>!s.preview).map(s=>({id:s.id,type:s.type,x:s.x,z:s.z,rot:s.rot||0}))};localStorage.setItem(SAVE_KEY,JSON.stringify(data))}
function die(){dead=true;fireHeld=false;saveGame();$('death-kills').textContent=player.kills;$('death-time').textContent=formatTime(runTime);$('death-day').textContent=parseInt($('day-value').textContent)||1;$('hud').classList.add('hidden');$('death').classList.remove('hidden');$('death').classList.add('show')}
function formatTime(s){return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(Math.floor(s%60)).padStart(2,'0')}`}

let touchMove={x:0,y:0},joyPointer=null,joyOrigin={x:0,y:0},lookPointer=null,lookLast={x:0,y:0};
function setupControls(){const joy=$('joystick'),stick=$('joy-stick');joy.addEventListener('pointerdown',e=>{AudioFX.resume();joyPointer=e.pointerId;joy.setPointerCapture(e.pointerId);const r=joy.getBoundingClientRect();joyOrigin={x:r.left+r.width/2,y:r.top+r.height/2};moveJoy(e);e.preventDefault()});joy.addEventListener('pointermove',e=>{if(e.pointerId===joyPointer)moveJoy(e)});const ju=e=>{if(e.pointerId!==joyPointer)return;joyPointer=null;touchMove={x:0,y:0};stick.style.transform='translate(-50%,-50%)'};joy.addEventListener('pointerup',ju);joy.addEventListener('pointercancel',ju);function moveJoy(e){let dx=e.clientX-joyOrigin.x,dy=e.clientY-joyOrigin.y;const mx=33,l=Math.hypot(dx,dy)||1;if(l>mx){dx=dx/l*mx;dy=dy/l*mx}touchMove={x:dx/mx,y:-dy/mx};stick.style.transform=`translate(calc(-50% + ${dx}px),calc(-50% + ${dy}px))`}
 const look=$('look-zone');look.addEventListener('pointerdown',e=>{AudioFX.resume();lookPointer=e.pointerId;look.setPointerCapture(e.pointerId);lookLast={x:e.clientX,y:e.clientY};e.preventDefault()});look.addEventListener('pointermove',e=>{if(e.pointerId!==lookPointer||paused)return;const dx=e.clientX-lookLast.x,dy=e.clientY-lookLast.y;lookLast={x:e.clientX,y:e.clientY};rotateLook(dx,dy,.0037)});const lu=e=>{if(e.pointerId===lookPointer)lookPointer=null};look.addEventListener('pointerup',lu);look.addEventListener('pointercancel',lu);
 bindHold($('fire-btn'),()=>{fireHeld=true;if(!W[player.weapon].auto)shoot()},()=>fireHeld=false);bindHold($('run-btn'),()=>runHeld=true,()=>runHeld=false);bindHold($('aim-btn'),()=>aimHeld=true,()=>aimHeld=false);$('reload-btn').addEventListener('pointerdown',e=>{e.stopPropagation();reload()});$('use-btn').addEventListener('pointerdown',e=>{e.stopPropagation();interact()});$('jump-btn').addEventListener('pointerdown',e=>{e.stopPropagation();jumpQueued=true});document.querySelectorAll('.slot[data-slot]').forEach((b,i)=>b.addEventListener('pointerdown',e=>{e.stopPropagation();switchWeapon(i)}));document.querySelectorAll('.slot[data-item]').forEach(b=>b.addEventListener('pointerdown',e=>{e.stopPropagation();useItem(b.dataset.item)}));window.addEventListener('keydown',e=>{keys.add(e.code);if(e.code==='KeyR')reload();if(e.code==='KeyE')interact();if(e.code==='KeyI')openInventory();if(e.code==='Space'){jumpQueued=true;e.preventDefault()}if(/^Digit[1-6]$/.test(e.code))switchWeapon(+e.code.slice(-1)-1);if(e.code==='Escape'&&started)togglePause(true)});window.addEventListener('keyup',e=>keys.delete(e.code));