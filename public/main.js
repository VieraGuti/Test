import * as THREE from 'https://esm.sh/three@0.185.0';
import { GLTFLoader } from 'https://esm.sh/three@0.185.0/examples/jsm/loaders/GLTFLoader.js';
import { Client } from 'https://cdn.jsdelivr.net/npm/@colyseus/sdk@0.17.43/+esm';
import { BUY_ITEMS, weaponById } from '/shared/weapons.js';
import { WORLD, SITES, OBSTACLES, DECOR, collides } from '/shared/map.js';

const $=id=>document.getElementById(id);
const touch=matchMedia('(pointer: coarse)').matches||navigator.maxTouchPoints>0;
const ui={login:$('login'),play:$('play'),name:$('name'),status:$('login-status'),hud:$('hud'),atk:$('atk-score'),def:$('def-score'),round:$('round'),timer:$('timer'),phase:$('phase'),hp:$('hp'),armor:$('armor'),money:$('money'),weapon:$('weapon-name'),ammo:$('ammo'),reserve:$('reserve'),reload:$('reload-state'),bomb:$('bomb-card'),progress:$('action-progress'),feed:$('feed'),buy:$('buy-menu'),buyGrid:$('buy-grid'),score:$('scoreboard'),rows:$('score-rows'),mobile:$('mobile')};
const ASSET='https://cdn.jsdelivr.net/gh/petroulacl/fps-asset-kit@main/';
const WEAPON_MODELS={sidearm:'weapons/flat_guns_east/Flat%20Guns%20East/GLB/Pistol_Compact_East.glb',smg:'weapons/flat_guns_east/Flat%20Guns%20East/GLB/SMG_Compact_East.glb',rifle:'weapons/flat_guns_east/Flat%20Guns%20East/GLB/Rifle_Assault_East.glb',sniper:'weapons/flat_guns_east/Flat%20Guns%20East/GLB/Sniper_Rifle_East.glb'};
const TEX={ground:ASSET+'textures/Ground054/Ground054.png',bricks:ASSET+'textures/Bricks097/Bricks097.png'};
let room,scene,camera,renderer,clock,mySid='',yaw=0,pitch=0,lastInput=0,lastFire=0,firing=false,localInit=false;
const pos=new THREE.Vector3(),keys=new Set(),actors=new Map(),moveTouch={forward:0,right:0};
let bombMesh,viewModel,weaponSocket,muzzleFlash,walkPhase=0,shotKick=0,muzzleUntil=0,currentWeapon='';
const weaponObjects=new Map();let matWall,matTunnel,matGround,matDoor,matCrate,matFar;
function initBuy(){for(const it of Object.values(BUY_ITEMS)){if(it.id==='sidearm')continue;const b=document.createElement('button');b.className='buy-item';b.innerHTML=`<b>${it.name}</b><span>$${it.price}</span><small>${it.id==='armor'?'100 ARMOR':'Weapon'}</small>`;b.onclick=()=>room?.send('buy',{id:it.id});ui.buyGrid.appendChild(b)}}initBuy();
ui.play.onclick=connect;ui.name.onkeydown=e=>{if(e.key==='Enter')connect()};$('close-buy').onclick=()=>ui.buy.classList.add('hidden');$('m-buy').onclick=()=>toggleBuy(true);$('m-reload').onclick=()=>room?.send('reload');
async function connect(){ui.play.disabled=true;ui.status.textContent='Cargando partida…';try{const proto=location.protocol==='https:'?'wss':'ws';const client=new Client(`${proto}://${location.host}`);room=await client.joinOrCreate('strike',{name:ui.name.value});mySid=room.sessionId;ui.login.classList.add('hidden');ui.hud.classList.remove('hidden');if(touch)ui.mobile.classList.remove('hidden');setup3D();bindNet();bindControls();preloadWeapons();feed('DUST TEST · assets 3D CC0','system');animate()}catch(e){ui.status.textContent=`Error: ${e.message||e}`;ui.play.disabled=false}}
function loadRepeatTexture(url,rx,ry){const t=new THREE.TextureLoader().load(url);t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(rx,ry);t.colorSpace=THREE.SRGBColorSpace;t.anisotropy=4;return t}
function setupMaterials(){const ground=loadRepeatTexture(TEX.ground,18,18),brick=loadRepeatTexture(TEX.bricks,3,2);matGround=new THREE.MeshStandardMaterial({map:ground,color:0xd4b17b,roughness:1});matWall=new THREE.MeshStandardMaterial({map:brick,color:0xc9b07f,roughness:.93});matTunnel=new THREE.MeshStandardMaterial({map:brick,color:0x806d57,roughness:.98});matDoor=new THREE.MeshStandardMaterial({color:0x563725,roughness:.86,metalness:.06});matCrate=new THREE.MeshStandardMaterial({color:0x8a5b31,roughness:.9});matFar=new THREE.MeshStandardMaterial({map:brick,color:0xa88e66,roughness:1})}
function setup3D(){scene=new THREE.Scene();scene.background=new THREE.Color(0xb8ddf0);scene.fog=new THREE.Fog(0xc9d9d6,62,115);camera=new THREE.PerspectiveCamera(75,innerWidth/innerHeight,.05,190);renderer=new THREE.WebGLRenderer({antialias:!touch,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,touch?1.25:1.8));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.05;renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;$('game').appendChild(renderer.domElement);clock=new THREE.Clock();setupMaterials();scene.add(new THREE.HemisphereLight(0xeaf6ff,0x6f5135,1.8));const sun=new THREE.DirectionalLight(0xfff0d0,2.8);sun.position.set(-30,45,-22);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);sun.shadow.camera.left=-55;sun.shadow.camera.right=55;sun.shadow.camera.top=55;sun.shadow.camera.bottom=-55;scene.add(sun);const ground=new THREE.Mesh(new THREE.PlaneGeometry(WORLD.half*2,WORLD.half*2),matGround);ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);for(const o of OBSTACLES)addStructure(o);for(const o of DECOR)addStructure(o,false);for(const [name,s] of Object.entries(SITES))addSite(name,s);addArrow('A →',19,2.25,-19,0);addArrow('← B',-19,2.25,-19,0);addArrow('A →',4,2.25,7,0);addArrow('← B',-4,2.25,7,Math.PI);addArchitectureDetails();bombMesh=makeBomb();scene.add(bombMesh);viewModel=makeViewModel();camera.add(viewModel);scene.add(camera);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}
function materialFor(kind){if(kind==='crate')return matCrate;if(kind==='door')return matDoor;if(kind==='tunnel')return matTunnel;if(kind==='distant')return matFar;return matWall}
function addStructure(o){const g=new THREE.BoxGeometry(o.w,o.h,o.d),m=new THREE.Mesh(g,materialFor(o.kind));m.position.set(o.x,o.h/2,o.z);m.castShadow=true;m.receiveShadow=true;scene.add(m);if(['sandstone','tunnel','distant'].includes(o.kind)){const trim=new THREE.Mesh(new THREE.BoxGeometry(o.w+.15,.2,o.d+.15),new THREE.MeshStandardMaterial({color:0xe2c68f,roughness:1}));trim.position.set(o.x,o.h+.08,o.z);trim.castShadow=true;scene.add(trim)}if(o.kind==='crate'){const edge=new THREE.LineSegments(new THREE.EdgesGeometry(g),new THREE.LineBasicMaterial({color:0x3b281a,transparent:true,opacity:.72}));edge.position.copy(m.position);scene.add(edge);for(const off of [-.38,.38]){const band=new THREE.Mesh(new THREE.BoxGeometry(o.w*1.01,.08,o.d*1.01),new THREE.MeshStandardMaterial({color:0x4e3422,roughness:.9}));band.position.set(o.x,o.h/2+off*Math.min(o.h,2),o.z);scene.add(band)}}return m}
function labelTexture(text,color='#fff',bg='rgba(80,25,10,.88)'){const c=document.createElement('canvas');c.width=256;c.height=128;const x=c.getContext('2d');x.fillStyle=bg;x.fillRect(0,0,256,128);x.strokeStyle='rgba(255,255,255,.3)';x.lineWidth=5;x.strokeRect(4,4,248,120);x.fillStyle=color;x.font='900 82px system-ui';x.textAlign='center';x.textBaseline='middle';x.fillText(text,128,65);const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;return t}
function addSite(name,s){const color=name==='A'?0xd75a30:0x3a79be;const disc=new THREE.Mesh(new THREE.CircleGeometry(s.radius,64),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.13,side:THREE.DoubleSide}));disc.rotation.x=-Math.PI/2;disc.position.set(s.x,.025,s.z);scene.add(disc);const ring=new THREE.Mesh(new THREE.RingGeometry(s.radius-.14,s.radius,64),new THREE.MeshBasicMaterial({color,side:THREE.DoubleSide}));ring.rotation.x=-Math.PI/2;ring.position.set(s.x,.04,s.z);scene.add(ring)}
function addArrow(text,x,y,z,rot=0){const p=new THREE.Mesh(new THREE.PlaneGeometry(2.5,1.2),new THREE.MeshBasicMaterial({map:labelTexture(text),transparent:true,side:THREE.DoubleSide}));p.position.set(x,y,z);p.rotation.y=rot;scene.add(p)}
function addArchitectureDetails(){const archMat=new THREE.MeshStandardMaterial({map:matWall.map,color:0xd4bb8d,roughness:.95});const lintel=new THREE.Mesh(new THREE.BoxGeometry(10.4,.8,1.35),archMat);lintel.position.set(0,5.18,1);lintel.castShadow=true;scene.add(lintel);const roof=new THREE.Mesh(new THREE.BoxGeometry(10.7,.42,25),matTunnel);roof.position.set(-32.5,5.55,-2.5);roof.castShadow=true;scene.add(roof);for(const x of [-4.5,4.5])for(const z of [0.35,1.65]){const bolt=new THREE.Mesh(new THREE.CylinderGeometry(.06,.06,.12,8),new THREE.MeshStandardMaterial({color:0x252525,metalness:.8,roughness:.4}));bolt.rotation.x=Math.PI/2;bolt.position.set(x,3.2,z);scene.add(bolt)}for(const [x,z,r] of [[-42,34,.75],[42,34,.75],[-42,-33,.7],[42,-34,.7]])makePalm(x,z,r);for(const [x,z] of [[-30,27],[-25,27],[25,30],[31,29],[-8,-20],[9,-20]])makeBarrel(x,z);for(const [x,z] of [[-34,18],[29,19],[-2,18]])makeSandbags(x,z)}
function makePalm(x,z,s=1){const trunk=new THREE.Mesh(new THREE.CylinderGeometry(.12*s,.2*s,3.4*s,7),new THREE.MeshStandardMaterial({color:0x725039,roughness:1}));trunk.position.set(x,1.7*s,z);trunk.castShadow=true;scene.add(trunk);for(let i=0;i<7;i++){const leaf=new THREE.Mesh(new THREE.BoxGeometry(.14*s,.025,1.8*s),new THREE.MeshStandardMaterial({color:0x4c7443,roughness:1}));leaf.position.set(x,3.45*s,z);leaf.rotation.y=i*Math.PI*2/7;leaf.rotation.x=.55;leaf.castShadow=true;scene.add(leaf)}}
function makeBarrel(x,z){const b=new THREE.Mesh(new THREE.CylinderGeometry(.36,.36,.9,18),new THREE.MeshStandardMaterial({color:0x4b5f63,metalness:.45,roughness:.55}));b.position.set(x,.45,z);b.castShadow=true;scene.add(b)}
function makeSandbags(x,z){for(let i=0;i<5;i++){const s=new THREE.Mesh(new THREE.CapsuleGeometry(.18,.55,4,8),new THREE.MeshStandardMaterial({color:0x8d8065,roughness:1}));s.rotation.z=Math.PI/2;s.position.set(x+(i%3-.9)*.55,.23+Math.floor(i/3)*.32,z);s.castShadow=true;scene.add(s)}}
function makeBomb(){const g=new THREE.Group();const body=new THREE.Mesh(new THREE.BoxGeometry(.55,.28,.42),new THREE.MeshStandardMaterial({color:0x242a28,roughness:.65}));g.add(body);const screen=new THREE.Mesh(new THREE.BoxGeometry(.24,.12,.025),new THREE.MeshBasicMaterial({color:0xff4c35}));screen.position.set(0,.03,-.225);g.add(screen);for(let i=0;i<3;i++){const wire=new THREE.Mesh(new THREE.TorusGeometry(.14+i*.025,.012,6,20,Math.PI),new THREE.MeshBasicMaterial({color:[0xe33b30,0x3ca15b,0xe5c744][i]}));wire.rotation.x=Math.PI/2;wire.position.set(0,.13,.02);g.add(wire)}g.visible=false;return g}
function makeViewModel(){const g=new THREE.Group();const skin=new THREE.MeshStandardMaterial({color:0x9f7452,roughness:.92}),glove=new THREE.MeshStandardMaterial({color:0x202322,roughness:.92});const armR=new THREE.Mesh(new THREE.CapsuleGeometry(.075,.38,5,10),skin);armR.rotation.z=-.48;armR.rotation.x=-.4;armR.position.set(.26,-.28,-.08);g.add(armR);const armL=new THREE.Mesh(new THREE.CapsuleGeometry(.07,.34,5,10),skin);armL.rotation.z=.72;armL.rotation.x=-.72;armL.position.set(-.1,-.25,-.55);g.add(armL);const gloveR=new THREE.Mesh(new THREE.SphereGeometry(.09,12,9),glove);gloveR.position.set(.14,-.16,-.2);g.add(gloveR);const gloveL=new THREE.Mesh(new THREE.SphereGeometry(.085,12,9),glove);gloveL.position.set(.02,-.13,-.62);g.add(gloveL);weaponSocket=new THREE.Group();weaponSocket.position.set(.05,-.03,-.5);g.add(weaponSocket);muzzleFlash=new THREE.PointLight(0xffb347,0,2);muzzleFlash.position.set(.05,.02,-1.25);g.add(muzzleFlash);g.position.set(.34,-.27,-.5);g.rotation.y=-.05;return g}
function preloadWeapons(){for(const id of Object.keys(WEAPON_MODELS))loadWeapon(id)}
function loadWeapon(id){if(weaponObjects.has(id))return;weaponObjects.set(id,null);new GLTFLoader().load(ASSET+WEAPON_MODELS[id],gltf=>{const holder=new THREE.Group(),root=gltf.scene;holder.add(root);root.traverse(o=>{if(o.isMesh){o.castShadow=false;o.receiveShadow=false;if(o.material){o.material.roughness=Math.max(.35,o.material.roughness??.6);o.material.metalness=Math.min(.75,o.material.metalness??.2)}}});let box=new THREE.Box3().setFromObject(root),sz=box.getSize(new THREE.Vector3());if(sz.x>=sz.y&&sz.x>=sz.z)root.rotation.y=-Math.PI/2;else if(sz.y>=sz.x&&sz.y>=sz.z)root.rotation.x=Math.PI/2;else root.rotation.y=Math.PI;box=new THREE.Box3().setFromObject(root);const center=box.getCenter(new THREE.Vector3());root.position.sub(center);sz=box.getSize(new THREE.Vector3());const target=id==='sidearm'?.48:id==='sniper'?1.0:.82;holder.scale.setScalar(target/Math.max(sz.x,sz.y,sz.z));holder.rotation.set(.02,-.06,-.02);holder.visible=false;weaponSocket.add(holder);weaponObjects.set(id,holder);if(currentWeapon===id)showWeapon(id)},undefined,()=>{weaponObjects.delete(id);feed(`No cargó modelo ${id}`,'system')})}
function showWeapon(id){if(currentWeapon===id&&weaponObjects.get(id)?.visible)return;currentWeapon=id;for(const [wid,obj] of weaponObjects)if(obj)obj.visible=wid===id;if(!weaponObjects.has(id))loadWeapon(id)}
function makeActor(team){const g=new THREE.Group(),bodyMat=new THREE.MeshStandardMaterial({color:team==='attackers'?0x76583d:0x33495d,roughness:.9}),gear=new THREE.MeshStandardMaterial({color:0x202527,roughness:.95}),skin=new THREE.MeshStandardMaterial({color:0xa47a59,roughness:1});const torso=new THREE.Mesh(new THREE.CapsuleGeometry(.28,.55,4,8),bodyMat);torso.position.y=1;g.add(torso);const head=new THREE.Mesh(new THREE.SphereGeometry(.22,12,10),skin);head.position.y=1.63;g.add(head);const vest=new THREE.Mesh(new THREE.BoxGeometry(.48,.5,.26),gear);vest.position.set(0,1.05,.02);g.add(vest);const leg1=new THREE.Mesh(new THREE.CapsuleGeometry(.095,.48,4,8),gear);leg1.position.set(-.14,.43,0);g.add(leg1);const leg2=leg1.clone();leg2.position.x=.14;g.add(leg2);g.traverse(o=>{if(o.isMesh)o.castShadow=true});return g}
function bindNet(){room.onMessage('event',e=>{if(e.type==='kill')feed(`${e.killer} ${e.headshot?'◉ ':''}${weaponById(e.weapon).name} ${e.victim}`,e.headshot?'head':'');else if(e.text)feed(e.text,'system')});room.onLeave(()=>feed('Desconectado','system'))}
function bindControls(){document.addEventListener('keydown',e=>{if(['KeyW','KeyA','KeyS','KeyD'].includes(e.code))keys.add(e.code);if(e.code==='KeyR')room?.send('reload');if(e.code==='KeyB')toggleBuy();if(e.code==='KeyE'&&!e.repeat)room?.send('use');if(e.code==='Tab'){e.preventDefault();ui.score.classList.remove('hidden')}});document.addEventListener('keyup',e=>{keys.delete(e.code);if(e.code==='KeyE')room?.send('useStop');if(e.code==='Tab')ui.score.classList.add('hidden')});renderer.domElement.onclick=()=>{if(!touch&&document.pointerLockElement!==renderer.domElement)renderer.domElement.requestPointerLock()};document.addEventListener('mousemove',e=>{if(document.pointerLockElement!==renderer.domElement)return;yaw-=e.movementX*.0022;pitch=Math.max(-1.25,Math.min(1.25,pitch-e.movementY*.0022))});document.addEventListener('mousedown',e=>{if(e.button===0&&document.pointerLockElement===renderer.domElement)firing=true});document.addEventListener('mouseup',e=>{if(e.button===0)firing=false});if(touch)bindTouch()}
function bindTouch(){const zone=$('move-zone'),base=$('move-base'),knob=$('move-knob');let id=null,c={x:0,y:0};zone.onpointerdown=e=>{id=e.pointerId;zone.setPointerCapture(id);const r=base.getBoundingClientRect();c={x:r.left+r.width/2,y:r.top+r.height/2};stick(e)};zone.onpointermove=e=>{if(e.pointerId===id)stick(e)};zone.onpointerup=zone.onpointercancel=e=>{if(e.pointerId!==id)return;id=null;moveTouch.forward=moveTouch.right=0;knob.style.transform='translate(0,0)'};function stick(e){let dx=e.clientX-c.x,dy=e.clientY-c.y,max=42,l=Math.hypot(dx,dy)||1;if(l>max){dx=dx/l*max;dy=dy/l*max}moveTouch.right=dx/max;moveTouch.forward=-dy/max;knob.style.transform=`translate(${dx}px,${dy}px)`}const look=$('look-zone');let lid=null,last={x:0,y:0};look.onpointerdown=e=>{lid=e.pointerId;look.setPointerCapture(lid);last={x:e.clientX,y:e.clientY}};look.onpointermove=e=>{if(e.pointerId!==lid)return;const dx=e.clientX-last.x,dy=e.clientY-last.y;last={x:e.clientX,y:e.clientY};yaw-=dx*.0052;pitch=Math.max(-1.25,Math.min(1.25,pitch-dy*.0052))};look.onpointerup=look.onpointercancel=e=>{if(e.pointerId===lid)lid=null};const f=$('m-fire');f.onpointerdown=e=>{e.preventDefault();firing=true};f.onpointerup=f.onpointercancel=()=>firing=false;const u=$('m-use');u.onpointerdown=e=>{e.preventDefault();room?.send('use')};u.onpointerup=u.onpointercancel=()=>room?.send('useStop')}
function movement(){return touch?moveTouch:{forward:(keys.has('KeyW')?1:0)-(keys.has('KeyS')?1:0),right:(keys.has('KeyD')?1:0)-(keys.has('KeyA')?1:0)}}
function animate(){requestAnimationFrame(animate);if(!room||!renderer)return;const dt=Math.min(clock.getDelta(),.05),p=room.state.players.get(mySid);if(p){if(!localInit){pos.set(p.x,0,p.z);yaw=p.yaw;pitch=p.pitch;localInit=true}showWeapon(p.weapon);const mv=movement();if(p.alive&&room.state.phase==='live')predict(mv,dt);pos.x+=(p.x-pos.x)*.12;pos.z+=(p.z-pos.z)*.12;camera.position.set(pos.x,1.55,pos.z);const cp=Math.cos(pitch);camera.lookAt(pos.x+Math.sin(yaw)*cp,1.55+Math.sin(pitch),pos.z+Math.cos(yaw)*cp);const speed=Math.min(1,Math.hypot(mv.forward,mv.right));walkPhase+=dt*(3+speed*7);shotKick*=Math.pow(.025,dt);if(viewModel){viewModel.position.x=.34+Math.sin(walkPhase)*.012*speed;viewModel.position.y=-.27+Math.abs(Math.cos(walkPhase*2))*.009*speed-shotKick;viewModel.rotation.x=-shotKick*1.7;viewModel.rotation.z=Math.sin(walkPhase)*.008*speed}if(muzzleFlash)muzzleFlash.intensity=performance.now()<muzzleUntil?3.2:0;const now=performance.now();if(now-lastInput>45){room.send('input',{...mv,yaw,pitch});lastInput=now}if(firing&&p.alive&&room.state.phase==='live'&&now-lastFire>30){room.send('shoot',{yaw,pitch});lastFire=now;shotKick=.045;muzzleUntil=now+45}}syncActors();syncBomb();hud();renderer.render(scene,camera)}
function predict(m,dt){let dx=Math.sin(yaw)*m.forward+Math.cos(yaw)*m.right,dz=Math.cos(yaw)*m.forward-Math.sin(yaw)*m.right,l=Math.hypot(dx,dz);if(l>1){dx/=l;dz/=l}const nx=pos.x+dx*6.2*dt,nz=pos.z+dz*6.2*dt;if(!collides(nx,pos.z))pos.x=nx;if(!collides(pos.x,nz))pos.z=nz}
function syncActors(){const seen=new Set();room.state.players.forEach((p,sid)=>{if(sid===mySid)return;seen.add(sid);let a=actors.get(sid);if(!a){a=makeActor(p.team);actors.set(sid,a);scene.add(a)}a.visible=p.alive;a.position.x+=(p.x-a.position.x)*.28;a.position.z+=(p.z-a.position.z)*.28;a.rotation.y=p.yaw});for(const [sid,a] of actors)if(!seen.has(sid)){scene.remove(a);actors.delete(sid)}}
function syncBomb(){const s=room.state,v=['dropped','planting','planted'].includes(s.bombState);bombMesh.visible=v;if(v){if(s.bombState==='planting'){const p=s.players.get(s.planter);if(p)bombMesh.position.set(p.x,.22,p.z)}else bombMesh.position.set(s.bombX,.22,s.bombZ);bombMesh.rotation.y+=.01}}
function hud(){const s=room.state,p=s.players.get(mySid);if(!p)return;ui.atk.textContent=s.attackersScore;ui.def.textContent=s.defendersScore;ui.round.textContent=`R${Math.max(1,s.round)}`;ui.phase.textContent=s.phase.toUpperCase();const end=s.bombState==='planted'?s.bombExplodesAt:s.phaseEndsAt,sec=Math.max(0,Math.ceil((end-Date.now())/1000));ui.timer.textContent=end?`${String(Math.floor(sec/60)).padStart(2,'0')}:${String(sec%60).padStart(2,'0')}`:'--:--';ui.hp.textContent=Math.ceil(p.hp);ui.armor.textContent=Math.ceil(p.armor);ui.money.textContent=Math.floor(p.money);const w=weaponById(p.weapon);ui.weapon.textContent=w.name;ui.ammo.textContent=p.ammo;ui.reserve.textContent=p.reserveAmmo;ui.reload.textContent=p.reloading?'RELOADING…':'';let bt='';if(p.hasBomb)bt='💣 LLEVAS LA BOMBA';else if(s.bombState==='dropped')bt='💣 BOMBA EN EL SUELO';else if(s.bombState==='planting')bt=`PLANTANDO EN ${s.bombSite}`;else if(s.bombState==='planted')bt=`⚠ BOMBA PLANTADA · ${s.bombSite}`;ui.bomb.textContent=bt;ui.bomb.classList.toggle('hidden',!bt);let val=0,txt='';if(s.planter===mySid){val=s.plantProgress;txt='PLANTANDO'}else if(s.defuser===mySid){val=s.defuseProgress;txt='DESACTIVANDO'}ui.progress.classList.toggle('hidden',!txt);if(txt){ui.progress.firstElementChild.style.width=`${val*100}%`;ui.progress.lastElementChild.textContent=`${txt} ${Math.round(val*100)}%`}const rows=[];s.players.forEach(x=>rows.push(x));ui.rows.innerHTML='<div class="score-row"><b>PLAYER</b><b>TEAM</b><b>K</b><b>D</b></div>'+rows.map(x=>`<div class="score-row"><b>${escapeHtml(x.name)}</b><span class="${x.team==='attackers'?'atk':'def'}">${x.team==='attackers'?'ATK':'DEF'}</span><span>${x.kills}</span><span>${x.deaths}</span></div>`).join('');if(s.phase!=='freeze')ui.buy.classList.add('hidden')}
function toggleBuy(force){const open=force??ui.buy.classList.contains('hidden');if(open&&room?.state.phase==='freeze')ui.buy.classList.remove('hidden');else ui.buy.classList.add('hidden')}
function feed(t,c=''){const e=document.createElement('div');e.className=`feed-item ${c}`;e.textContent=t;ui.feed.prepend(e);while(ui.feed.children.length>6)ui.feed.lastChild.remove();setTimeout(()=>e.remove(),6000)}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
